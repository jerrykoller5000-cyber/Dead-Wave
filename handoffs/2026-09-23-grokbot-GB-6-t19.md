# GB-6 t19 — hunt swarm / base (was 120s probe)

## Result
**6 pass, 0 fail** (~38s, was a 120s soak with no asserts)

## Touch
- `tools/tests/t19.js` only

## Changes
Same cause as t5–t10: empty callsign → no match → no zombies.
1. Callsign + prep + far stick.
2. Assert `devBaseBuild` succeeds and keep has floors.
3. `skipGrace` + swarm; assert ≥5 alive zombies within 15s.
4. Dropped the 120s info soak (Claude: give probes a time limit).

## Next
GB-6 t36
