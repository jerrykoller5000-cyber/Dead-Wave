# cursor — CU-42 the 48-zombie frame and night 1's slow frame — 2026-09-25
Changed: No game code. Profiled on Jerry's RTX 5080 (WebGPU, visible window). The 48-zombie frame is CPU-bound on three.js scene traversal and draw submission, not zombie AI and not the GPU. Night 1 has two one-off frames of about 67 ms.
Files: qa/run-cu42.mjs, handoffs/2026-09-25-cursor-CU-42.md
Tests:
- `node qa/run-cu42.mjs --night 7` with 48 alive (5 within 20 m, 41 beyond 60 m): 480 frames at 54.6 fps, 99th percentile 33.4 ms, 49 frames over 25 ms, 897 draw calls. The main thread was busy 99% of the profile: `tick` 17.4 ms/frame, of which `renderFrame` 14.9 ms (`updateMatrixWorld` 2.4, `_projectObject` 2.2, `_renderObjectDirect` 1.0, `multiplyMatrices` 0.8, `writeBuffer` 0.6 ms/frame). `updateZombies` is 1.0 ms/frame; `resolveStaticProps` 0.3.
- A second run read 39-47 fps under the profiler with 853 draws. The night fog is 18-65 m, so the 41 zombies past 60 m are drawn into fog. Hiding them from outside the zombie code does not work: the game sets each zombie mesh's visibility every frame (my experiment still drew 48 of 48).
- Night 1, first 25 s after `hqStartWave`: worst 66.7 ms at 6.0 s (the wave's start, 2 alive, no new shader variant) and 66.7 ms at 17.7 s (the frame after one new shader variant, 15 alive). The old 82 ms frame is now about 67.
Screenshots: none.
Not verified: a fog cull's real gain (it needs to go inside the zombie update); what exactly first-uses at the 6.0 s wave start (music section, cave dust at warn level 2, or the banner) and which material compiles at 17.7 s.
Requests: Grokbot, a distance cull in the zombie update: skip drawing (and animating) zombies past the night fog's far distance plus a margin; about 41 of 48 are past 60 m at the cap. Claude and Grokbot, the two night-1 frames above.
Contract changes: none.
