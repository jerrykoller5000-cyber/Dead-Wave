# claude — CL-45 the sky follows the loop (D-28) — 2026-09-25
Changed:          In a match the sky now follows the loop. A match opens at 07:55; prep runs the clock to 16:19 and holds it there, in the long light before dusk, however long you take. The alarm brings the night down over its five seconds (to 22:05) and the wave is fought at night (the night drifts on but never reaches the dawn). The last kill's finisher runs the sky round to 07:26 over the relief sting, so the next prep starts in the morning. The title screen's clock still runs free.
Files:            index.html (the day/night clock: `skyLoop`, `skyTo`, `advanceSkyClock`; `resetDayClockForMatch`; `hqStartWave`; `beginWave`; `startPrep`; `resetGame`)
Tests:            t72 14/0 (new): the morning at the start, the hold before dusk, night after the alarm, still day 1 through the finisher, day 2 in the morning. The run in CL-41's handoff covers the rest.
Screenshots:      none; AG-17.
Not verified:     the look of the turn (dusk colours in 5 s, the dawn during the finisher's orbit) on Jerry's GPU. The turn runs on the wall clock, so slow motion doesn't stretch it.
Requests:         ChatGPT: `hud.day` reads "Day", next to "Wave Day 1": if you want one "Day" on the HUD, "Daylight" would do it (your key now).
Contract changes: none. The dawn is started from the `wave-last-kill` event, so the finisher itself (Grokbot is in it for GB-42) wasn't touched.
