# cursor — CU-36 first-use stalls and the title time — 2026-09-25
Changed: The load warm-up now carries one skull of each kind and turns the goggles' CSS filter on for its frame, so neither compiles or composites for the first time mid-game. The warm-up log line now times its four parts. Also fixed the test harness so the new bare `import './ui/camp-cleared.js'` loads in the test page (caeb85f); every check had been failing to load.
Files: index.html (load warm-up), tools/tests/run-all.mjs (pushed in caeb85f)
Tests: On Jerry's GPU (WebGPU, visible window):
- First goggles toggle at night: worst frame 16.8 ms, every frame about 17 ms (was one 200 ms frame, CU-35).
- First finisher of the session: worst frame 33.5 ms, 0 frames over 50 ms (was about 1 s headless / 373 ms on GPU, CU-29). One new shader variant still builds there, cheaply.
- `node tools/tests/run-all.mjs t45 t61 t71 t62 --jobs 2` → 8/0, 18/0, 16/0, 4/0.
- `node tools/loadtime.mjs --runs 2` → title 5.8 s and 7.4 s warm. The warm-up stage: post 3-9 ms, pools 65-103 ms, compile 1702-2506 ms, then the warm frame. The shader compile is the whole cost.
Screenshots: none. Nothing visible changed.
Not verified: a before measurement in this session (I briefly stashed index.html to get one, stopped within seconds, and restored it; its diff was only my lines). The warm title is still over 5 s.
Requests: none. Q-1 is answered by D-36: the load budget is met by the splash, and CU-36 keeps only the stall fixes. The index.html change goes in with the next CU-37 batch (ChatGPT and Grokbot are in the file).
Contract changes: none.
