# grokbot - GB-34 t34 poll fixed waits - 2026-09-24
Changed:          TEST-ONLY. Added Claude's `until(cond, ms)` helper (as t60/t61) to t34 and replaced the fixed wall waits on the HQ deposit sequence (throw/process/green) and the alarm/wave/flare waits. Assertion thresholds unchanged. Game code untouched. Root cause: deposit/alarm advance on game time; under `--jobs 2+` hitching, fixed 900ms left dep still `open` ("thrown in (open)").
Files:            tools/tests/t34.js
Tests:            npm test -- t34 --jobs 1 -> 20 pass / 0 fail (30.4s). npm test -- t34 --jobs 2 -> 20/0 (29.9s). npm test -- t34 --jobs 3 -> 20/0 (30.1s). Under load: npm test -- t34 t35 t59 t60 t61 --jobs 3 -> t34 20/0 (32.3s) while siblings ran in parallel (t60/t61 failed on unrelated opening-screen music; not this task).
Screenshots:      none
Not verified:     Did not broaden to every combat test with a long `await wait(N)`. Candidates left for a later GB if they flake under load: t35 supply-crate fade/clear waits (900/6500/4000), t8 (6000/4000), t15 (5000), t29 (2500), t39 (2500/5500/9000). Prep-holds 2500ms in t34 intentionally kept (proves nothing triggers).
Requests:         none
Contract changes: none

## Follow-up (Claude review ~12:57Z)
Addressed: `GB-34 t34: under --jobs 3 down from 5 fails to 1 ('ding: light green, +$0' still a fixed wait) -> Grokbot, poll that one too.`
Changed:          The green wait only polled `T.hq.dep === 'green'`. Under `--jobs 3` cash can land a tick later, so the assert saw green with `+$0`. Now: `await until(() => T.hq.dep === 'green' && T.getBank() === bank0 + val, 12000)`. Assertion text unchanged. Game code untouched.
Files:            tools/tests/t34.js
Tests:            npm test -- t34 --jobs 1 -> 20 pass / 0 fail (30.3s). --jobs 2 -> 20/0 (29.9s). --jobs 3 -> 20/0 (29.8s). Ding flake gone.
Screenshots:      none
Not verified:     Did not re-run the earlier multi-file load suite; single-file jobs 1/2/3 cover the reported flake.
Requests:         none
Contract changes: none
