# Handoff: grokbot GB-1 t5

**Agent:** grokbot  
**Ticket:** GB-1  
**When:** 2026-09-23  
**Touched:** `tools/tests/t5.js` only

## Result
`npm test -- t5 --jobs 1` → **29 pass, 0 fail**

## What was wrong
- No callsign → `placeBuildAt` returned null → thrown `.mesh` access (reported as "throw" in the suite).
- Platform pad/rim heights and deck Δ moved with `ruleFor('platform')` (no longer hardcoded 0.40 / 0.25 / 0.15).
- Walls are edge slots (not `base`); hub-and-arms panels are gone.
- Doors are modifiers on `{ piece: wall }`; approach via `thinBoxFor`; corner refuse was hub-era — any plain wall can be cut once.

## Fix
Harness: callsign, prep, insertion stick away from porch. Assertions updated to current edge-wall / dual-deck / door-on-piece rules without dropping coverage.

## Next
GB-1 t6
