# cursor — CU-4 the split, paused — 2026-09-23
Changed: Nothing new this checkout. The split is paused after two slices so the freeze can come off.
Files: core/math.js (b4c670d), core/geometry.js (94940f3). index.html still holds the rest.
Tests: t45 was 8 pass after each slice. Full npm test not re-run for this pause.
Screenshots: none for the pause.
Not verified: the rest of the carve (audio, boot, world, life, combat, ui).
Requests: none new.
Contract changes: math and geometry sections added to docs/contracts.md in those two commits. Claude keeps that file (CL-13).
