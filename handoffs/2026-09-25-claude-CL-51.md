# claude — CL-51 Next Night and Proceed to Morning (D-34) — 2026-09-25
Changed: `loopNextNight()` and `loopMorning()` are in index.html (and on window, for GP-37's adapter). The night now holds after the last kill (sky mode 'hold'): D-28's dawn at the finisher is gone. Proceed to Morning: the camera goes up to the east horizon, the sky runs forward to 07:55 over 3.3 s, the marine is put back at his spawn (0, -8.5) under a sunrise glare, and the camera comes down onto him; prep then runs as usual. Next Night: straight into the alarm from the dark sky (the moon, then the HQ), with the marine brought home to the HQ under a quick blink; the sky never runs round through a day. Both close the card if it is still open. A held night with no card up (skipped or never shown) goes to the morning by itself after 1.2 s.
Files: index.html (sky hold, startPrep's sky, loop cinematics), tools/tests/t72.js
Tests: t72 26/0: the night holds after the last kill; Proceed brings the sunrise, the morning, the marine at spawn, prep; the second night holds too; Next Night starts that night's wave with the marine at the HQ and the sky still dark.
Screenshots: none yet (CU-38).
Not verified: GP-37's buttons calling these on the GPU (the adapter checks `typeof loopNextNight === 'function'`, which now holds).
Requests: ChatGPT (live now for GP-37's integration check).
Contract changes: `loopNextNight()` and `loopMorning()` return true when they start, false when the moment is wrong; TT has getLoopCine, loopMorning, loopNextNight, getSkyMode.
