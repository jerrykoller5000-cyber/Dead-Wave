# GB-1 t7 — door leaves at two yaws + platform on wall

## Result
dump / npm path → **16 pass, 0 fail**

## Touch
- `tools/tests/t7.js` only

## Changes
Was a hub-era dump (`doorAxis`, `hubPost`, no `ok()`). Rewrote as live-match asserts:
1. Callsign + prep + far stick.
2. Door via `{ piece: host }` on edge walls for yaw 0 and 1.
3. Assert `doorFrame` / `doorLeaves`, no `hubPost`, leaf swings past 1.4 after `updateDoors`.
4. Platform on wall with `{ lv: 1 }`.

## Next
GB-1 t9
