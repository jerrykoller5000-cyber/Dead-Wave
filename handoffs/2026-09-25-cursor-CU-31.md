# cursor — CU-31 no morning save; ways to die kept — 2026-09-25
Changed: Took out the morning save (D-30): `writeDayStart`, `applyDayStart`, `readDayStart`, `clearDayStart`, the `tt_day_start` key, the objective-restore hook, and the `quietPrep` flag. A new game no longer clears `tt_death_log`, so the ways-to-die collection lasts across runs (D-31).
Files: index.html (saves, death log), tools/tests/t62.js
Tests: `node tools/tests/run-all.mjs t62 t45 t35 --jobs 3` → t62 4/0, t45 8/0, t35 28/0. t62 now checks that no morning save is written, the save hooks are gone, and two planted deaths survive a new game. The old t62 expectations (save round-trip, cleared death log) are removed, so this is a changed test (review).
Screenshots: none. Nothing visible changed.
Not verified: a player's browser that still holds an old `tt_day_start`. Nothing reads it any more, so it is harmless, but it is not deleted.
Requests: none.
Contract changes: none. `TT.saveDayStart`, `TT.loadDayStart`, `TT.clearDayStart` are gone (tests only used them).
