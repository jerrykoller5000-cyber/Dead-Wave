# claude — CL-64 The guardian's grab and drag, the first scene; the game plays it — 2026-09-26
Changed: The guardian's catch is a studio scene now, and the game plays that file. `studio/scenes/guardian-grab-drag.json` (made by `studio/make-guardian-grab.mjs`, the turn solved by `studio/tune-grab-turn.mjs`): the marine runs from it; the guardian, facing him (Jerry's note: a creature grabs facing its target, aiming for the leg, whichever way he lies), lunges low and takes his left ankle in its right hand; it rears back and yanks him off his feet onto his face; it walks its body round the leg it holds to face the cave (so he isn't swung through the air), and hauls him away at a walking drag, its steps matched to the ground. New clips: guardian/lunge, pull, turn, haul; marine/flee, fall, dragged. In the game (index.html) the cave drag starts that scene when it's loaded (`startGrabScene`): the scene is laid at the real grab spot facing the real mouth, the haul path runs to the mouth and into the cave, bodies stand on the real ground, and the game's guardian and marine are adopted and eased in from where they really were. The old code path stays as the fallback (scene not loaded yet, or it can't start); the camera, blood, dust, sounds and the aftermath are the same.
Scene player additions (docs/studio.md §9): keyed `at`; `onPath` (blend from `at` onto a path); `aim` (face a joint of another body, and which way round to turn away); holds' `trail` (the towed body swings round behind the grip), `taut` (the held leg is pulled straight) and "hung by it"; a `step` channel (a limb steps for itself while the scene moves the body under it); `enter`/`paths`/`ground` host options; `sp.path(name)`, `sp.dispose()`; adopted bodies take the studio rest pose; `fetchScene(name)`. Late in the task, for the in-game pops: a held limb lets go of its clip's plant by as much as it's held (was all at once), and is pulled straight along the clip's direction; a body easing in from gameplay doesn't pin its feet until it has arrived; every blend turns the short way (`slerpTo`: the tests' stand-in three blends quaternions component by component, which swung the marine's hip 0.8-0.95 rad the long way at a partial weight, now and then).
Files: index.html (cave drag: startGrabScene/updateGrabScene/endGrabScene, TT.caveGrabReady), studio/scene.js, studio/clip.js, studio/ik.js, studio/rigs.js, studio/marine.js, studio/load.js, studio/index.js, studio/make-guardian-grab.mjs, studio/tune-grab-turn.mjs, studio/scenes/guardian-grab-drag.json, studio/clips/guardian/{lunge,pull,turn,haul}.json, studio/clips/marine/{flee,fall,dragged}.json, studio/scene.test.mjs, studio/clip.test.mjs, tools/tests/t84.js (new), tools/tests/t79.js, docs/studio.md, docs/contracts.md, review/guardian-grab-drag/ (v1, v2)
Tests: studio `node --import ./studio/node-three.mjs --test "studio/*.test.mjs"` 19/19 (new: a held foot eases off its pin by how much it's held, and blends turn the short way). Guardian bake check passes and the eight baked clips are unchanged. Game: t84 (new) 10/0: the scene loads, the chase catches him, the scene drives the drag, the hand holds the ankle, he's hauled more than a metre and ends at the cave, it lets go at the cut, the rig is back on its hanger and the marine's torso and hips are back as they were. t59 48/0 (the guardian's whole chase and drag). Full suite at --jobs 3 on this 2-core clone: 18 files failed from load (timeouts, "chase running" never seen); every one of them passed when run again one at a time, except t79.
Not verified: t79 (A) is on the line: the biggest one-frame turn in the chase, catch and drag is 0.30-0.34 rad against its 0.3 limit (the final version: 0.313 and 0.342 fail, the third run passes 12/0), at m.kneeLG / m.legLG 0.6-0.67 s, the yank. It was 0.44 at the start of this fix, and 0.83-0.95 on the runs that hit the long-way blend. The yank is where the marine's left leg is taken off the ground and pulled straight; his running legs before it sit at 0.26-0.3 themselves. `--review` for this. Not seen on Jerry's GPU. The studio's own flags on this scene (the review stats): the guardian's legs snap once each on the haul (0.48-0.52 rad at 2.33 and 2.87 s), its planted left hand and feet slide 0.17-0.33 m during the turn, the hand is 7 cm off the ankle at the moment of the grab, and the marine is thrown at up to 12.5 m/s during the yank (not a check window; it reads as violent in the video, which may be right).
Screenshots: review/guardian-grab-drag/v2/strip.png, review/guardian-grab-drag/v2/video.webm
Requests: Cursor: `tools/studio.mjs scene` waits 120 s for the strip and 180 s for the video; this 5.5 s scene takes about 450 s to record on the cloud clone (four passes at 1x and 0.25x), so both were raised on a throwaway copy to render it. Scale the waits with the scene's length (the video is 2 x (length + length/0.25) seconds of real time).
Contract changes: docs/contracts.md, the studio section: the game plays guardian-grab-drag through `Studio.fetchScene` + `createScene` with `paths`/`ground`/`enter`, and hands the bodies back with `dispose()`.
