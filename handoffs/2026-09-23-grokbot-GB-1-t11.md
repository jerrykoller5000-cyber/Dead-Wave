# Grokbot GB-1 — t11 turret / decks

**Date:** 2026-09-23  
**Agent:** grokbot  
**Task:** GB-1 (failing combat tests)  
**Touched:** `tools/tests/t11.js` only (no `index.html` change)

## What

Updated the placement harness so it matches current build rules.

## Why the old test failed

1. **Turret on raised pad** — pad height is `RAIL_H * 0.75` (= 0.54), not the old hardcoded 0.4. Assertion now uses `T.ruleFor('platform').height` (and rim via `edgeH`).
2. **Platform on a floor** — with the marine on the ground, a plain `placeBuildAt('floor')` boardwalks at level 0. Scripted floor-on-walls needs `{ forceLv: 1 }` so the platform bolts on at level 2.
3. **No floor on a platform** — ground boardwalk under a tower is allowed; standing *on* a platform deck is refused. Assert `resolveTarget` / `placeBuildAt` with `{ forceLv: 2 }` and a refusal that mentions platform.
4. **Callsign** — Play needs a name; wait until `getPhase() === 'prep'` (same lesson as t18).

## Result

`npm test -- t11 --jobs 1` → **13 pass, 0 fail**.

## Next

GB-1 next file: t12 (or whichever fails first in the Claude list after t11).
