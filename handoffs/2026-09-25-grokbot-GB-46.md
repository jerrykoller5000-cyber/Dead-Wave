# grokbot - GB-46 The four build-placement oddities from GB-45: two were bugs, two are meant - 2026-09-25
Changed: (1) t8 was a test bug: it now starts a real match and checks the wave is on. (2) Aimed placement works in a started match; the menu readings were `tryPlace`'s own not-started guard, and the aim wasn't one cell off. That check turned up a real bug, which I fixed: a turret aimed at a deck that already carries one used to drop to the ground under that deck, and you paid for it. Now it is refused. (3) Meant: the cabin refusal against placeBuildAt is the player's check against the unchecked grid primitive. (4) Meant: with no aim a turret takes the lowest free spot, and a floor with no aim goes on the ground (D-12), so a railing can't have the wall's edge. t1, t2, t3, t4 and t8 now assert all of this.
Files: index.html (resolveTarget: one new branch for object pieces aimed at a deck); tools/tests/t1.js, t2.js, t3.js, t4.js, t8.js
Tests: t1 4/0, t2 4/0, t3 5/0, t4 5/0, t8 3/0. Build set after the fix: t0 2/0, t5 31/0, t6 20/0, t6a 2/0, t7 16/0, t9 19/0, t10 30/0, t11 13/0, t12 22/0, t13 8/0, t14 13/0, t17 10/0, t21 16/0, t24 14/0, t25 29/0, t29 12/0, t31 26/0, t33 4/0, t49 32/0, t51 19/0, t58 8/0 (346 pass)
Screenshots: none
Not verified: in play, by hand. Two things I did not change: a turret aimed at the deck the marine stands on is placed under his feet (t2's third trial places a flame on his own cell), and t30 is another probe with no assertions (turret at 4,-4).
Requests: none
Contract changes: resolveTarget / placeBuildAt / tryPlace: a lure, mortar or turret (`slot: 'object'`) aimed at a deck (opts.lv > 0) goes on that deck or is refused ("cell taken"). It no longer falls back to another level.

## Detail
- (1) t8: Play without a callsign never starts the match, so skipPrep and skipGrace did nothing and it ended on "phase idle, zombies 0". t8 now uses `startMatch`, builds after the insertion, and asserts:
  - 13 new builds, with 5 of 5 platforms at lv1;
  - the door went in and stands open;
  - the wave came (phase wave, 15 alive).
- (2) t2 and t4 were placing on the menu. `tryPlace` returns straight away when `!gameStarted`, so valid=false placed=0 was that guard. The marine also stands in the cabin there (cabin at 0,0, 5 m each way), which is where the one-cell-off aim came from.
  - Both now run in a started match, on cells that pass the player's own wall check (clear of the cabin, trees and water).
  - t4: a light aimed at each of three decks lands on that deck (cell 0,0, lv1).
  - The bug: a heavy aimed at the same decks used to go in at lv0, on the ground under each deck, and was charged (seen in a probe: `NEW heavy lv0 object`). `resolveTarget` tried the aimed level first and then every level from 0 up.
  - The fix: for `slot: 'object'` pieces with an elevated aim (opts.lv > 0), only the aimed level is tried. Now t4 asserts the heavy is refused (ghost red), nothing is placed, and there's nothing under the decks. This is the turret version of the floors' GB-8 rule: never quietly somewhere else.
  - Aiming at the ground (lv0 or no aim) is unchanged.
  - t2: from the real camera on the ground, and from the next deck, the light and the heavy go on the decks. Its old made-up ray origin, 6 m behind him, missed the deck and hit the ground one cell past.
- (3) Meant. `placeBuildAt` is the grid primitive (no cabin, water or rough-ground checks; scripted builds and every test use it). `placeRefusalFor` / `placeRefusal` is the player's check that `tryPlace` runs. In t1 (menu, marine in the cabin) the light's spot touches the cabin, so the player is refused while the primitive places it. t1 asserts both, and that a light aimed at the deck (opts.lv 1) resolves to the deck.
- (4) Meant.
  - With no aim, a turret takes the lowest free spot. On a wall+platform cell that is the ground under the deck, where there is headroom. Aimed at the deck (which is what the reticle gives), it goes on the deck. t3 asserts both.
  - A floor with no aim is laid on the ground (D-12) inside the wall. The wall then owns that edge, so a railing is refused there but goes on edge1. Aimed at the wall top (opts.lv 1), the floor roofs the wall and takes a railing. t3 asserts all three.
  - The refusal message for the wall's edge reads "needs a platform or floor under it" (a higher level's reason wins over "that edge already has one"). That's a little misleading, but I left the message rules alone.
- t14 failed 2 ("row lines up") in the baseline run before my change and passed 13/0 after it. It looks flaky, not caused by this.
- I used a throwaway probe (tgb46) and a raw-output copy of run-all. I deleted both and removed the probe's key from crew/tests.json.
