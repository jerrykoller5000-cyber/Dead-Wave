# Grokbot — GB-5 floors stairs bridges cover — 2026-09-23

Changed:          Verified floors, stairs, bridges and cover match walk/collide/block
                  behaviour, and shipped what was wrong: soft barricade shove had a
                  zero-distance singularity (body exactly on the thin line got no push).
                  New `tools/tests/t49.js` asserts one clear check per area. Also wired
                  D-8 `purchase-delivered` on paid tryPlace / drag place / shovel /
                  tryUpgrade / upgrade-drag via existing `reportPurchase(..., 'build'|'upgrade')`.
                  Exported `entityGroundY`, `resolveStaticProps`, `climberCanVaultBuild` on TT.
Files:            `index.html` (builds collision + purchase emit + TT exports);
                  `tools/tests/t49.js` (new);
                  `handoffs/requests.md` (D-8 purchase-delivered → DONE)
Tests:            `npm test -- t49 --jobs 1` → **32 pass, 0 fail** (~19.7 s CT);
                  `npm test -- t12 --jobs 1` → **22 pass, 0 fail** (~30.2 s CT) stairs/floors smoke.
Screenshots:      none (harness probes).
Not verified:     Live GPU feel of bridge approach grades / stair fold; flow-field still
                  marks vaultable low cover blocked for climbers (collision vault only —
                  same Phase-1 note as GB-4); under-bridge zombie pathing at every river
                  sample (one bridge probed).
Requests:         ChatGPT D-8 build/upgrade `purchase-delivered` → **DONE** (also covers
                  board GB-11 emit sites). Floors/cover trustworthy item from Jerry order 7.
Contract changes: TT exports: `entityGroundY`, `resolveStaticProps`, `climberCanVaultBuild`.
                  `reportPurchase(itemId, cashSpent, 'build'|'upgrade')` after successful
                  paid place/upgrade (shovel dig included as build). Soft barricade: when
                  `dist≈0` inside the thin slab, shove along the short-face normal.

## t49 coverage

- Floors: deckY == platformHeightAt == entityGroundY; floor does not hard-shove on surface.
- Stairs: ramp rise meets roof; mid-ramp walk height; sides block below surface; on-ramp no shove.
- Bridges: landmark platform; centre walk height; under-channel ground; rails shove on deck only.
- Cover: climber vaults sandbag/wire not barricade/wall; hard block / vault skip; soft barricade
  shove; sandbag/barricade segmentHitsBuild stop chest shots; wire low-cover geometry.
- D-8: tryPlace sandbag fires purchase-delivered with matching bank debit.

## Remaining (honest)

Flow-field climber vault (GB-9 adjacent). No save-each-day work (out of scope this session).
