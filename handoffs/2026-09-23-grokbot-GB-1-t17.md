# Grokbot GB-1 — t17 reticle through roof / scrap

**Date:** 2026-09-23  
**Agent:** grokbot  
**Task:** GB-1  
**Touched:** `tools/tests/t17.js` only

## What

Updated the harness for current floor levels, a live match, and the known flaky scrap ray.

## Fixes

1. **Callsign + prep + insertion wait** — placement / aim / cutaway need a live match.
2. **Upper floors / roof** — `{ forceLv: 1 }` instead of floating `p.y += 3` (plain place boardwalks at 0).
3. **Scrap ground-first** — retry `scrapTarget` a few frames with a fresh aim ray (Claude's flaky check under load).
4. **Fade materials** — read `o.material.opacity` only when `o.material` exists; wait long enough for cutaway ease.

## Result

`npm test -- t17 --jobs 1` → **10 pass, 0 fail**.

## Next

GB-1 t21.
