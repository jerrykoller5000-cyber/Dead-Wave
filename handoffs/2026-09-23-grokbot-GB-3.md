# GB-3 — getWavePreview(day) · 2026-09-23

Changed:          Wave plan is frozen once in `startPrep()` as `wavePreview` + parallel
                  `caveByIndex`. `spawnWaveBatch` consumes that plan (same type order and
                  cave assignment). Ambush option **A**: retarget only to a mouth already in
                  `wavePreview.caveIndices`. Exported `getWavePreview`, `getWaveDirectorState`,
                  `getActiveCaveIndices` (plus `spawnWaveBatch`/`beginWave` for tests) on
                  `window.TT`. `nodead` clears the plan.
Files:            index.html (wave director only: state, startPrep, spawnWaveBatch, getters,
                  TT exports, nodead clear);
                  tools/tests/t43.js (new)
Tests:            `npm test -- t43 --jobs 1` → **33 pass, 0 fail** (~8s). Did not re-run the
                  full suite / t19 (out of scope; spawn path only retargets cave pick).
Screenshots:      none (data-plane change; no UI).
Not verified:     Live GPU ambush feel; ChatGPT preview panel wiring; caveWarn emit at prep
                  (CL-4 / GB-7). Surround days list all used mouths in `caveIndices` (usually
                  all six) rather than leaving the array empty — `surround` flag is the UI
                  signal.
Requests:         ChatGPT: consume `getWavePreview()` for the prep panel / minimap.
                  Claude: call `caveWarn` from prep when ready (indices = getActiveCaveIndices()).
Contract changes: **WavePreview** on director: `{ day, bloodMoon, surround, hasColossus, total,
                  caveIndices, bearings, byTypeAndCave:[{typeKey,count,caveIndex,caveTheme,caveName}],
                  queue, caveByIndex }`.
                  Exports: `getWavePreview(day?)`, `getWaveDirectorState()`, `getActiveCaveIndices()`.
                  Ambush: **A** (preview-honest). Spawn match: success path shifts `waveQueue` and
                  `caveByIndex` together; field-full backoff does not drop/reorder.

## Next
GB-7 (aim in-game / follow-ups on board).
