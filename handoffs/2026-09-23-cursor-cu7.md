# cursor — CU-7 pit camera — 2026-09-23
Changed: The `pit` shot now uses Claude's camera: twelve metres out, sixteen above the old water line, looking five metres under the funnel floor, field of view 50. A behaviour check that is still running after 75 seconds is recorded as could-not-run, so a probe cannot sit for two minutes.
Files: tools/shoot.mjs, tools/tests/run-all.mjs
Tests: `node tools/tests/run-all.mjs t45 --jobs 1` → 8 pass, 0 fail, 6.8 s. Full `npm test` not re-run. The 75 s path was not the one t45 hit.
Screenshots: none from here. `pre-split/pit.png` is not in the repo, so `--compare` had nothing to diff. Antigravity has the shot (AG-2); request sent.
Not verified: the picture of the rune ring; a check that actually runs into the 75 s limit.
Requests: Antigravity, shots of the pit with the new camera.
Contract changes: none.
