# Cursor — CU-1 Apply loader and merge patches — 2026-09-23

Changed:          Applied Claude's loader patch, then the fast `mergeParts` patch, to
                  `index.html`. A background tab now reaches the title screen. The
                  foreground wait is unchanged, which is CU-2.
Files:            `index.html` (boot / load channel, `mergeParts`)
Tests:            Spot checks, one page at a time: t11 13 pass, t18 7 pass, t39 15 pass,
                  t45 8 pass. A two-wide `npm test` is not comparable to the last full run:
                  hidden pages often never publish `window.TT` (see Not verified).
                  Last full run before this patch: 215 pass, 58 fail, 9 could not run.
Screenshots:      none. Neither patch changes what is drawn. `mergeParts` is claimed
                  bit-for-bit the same mesh; the loader only changes when the title appears.
Not verified:     Cold cache. GPU memory during the hidden pre-roll pump. A clean full
                  `npm test`: with two pages, the one that is hidden often sits 120 s and
                  reports "window.TT never appeared". Alone, the same check passes in
                  seconds (t45: 120 s in the crowd, 8.1 s alone). Likely the hidden-tab
                  pump calling `tick()` once, throwing under fakethree, and then stopping,
                  while a hidden page also gets no real frames. Real Chrome in a real
                  hidden tab does finish (7.5 s below).
Requests:         none new. CU-2 is the title gate (D-2).
Contract changes: `window.DWLoad`, already approved by Claude on 2026-09-23
                  (`handoffs/2026-09-23-claude-loader.md`). Copying it into
                  `docs/contracts.md` is CU-3.

## Load, Jerry's PC, Chrome, real GPU, warm cache

| | before (unpatched) | after |
| --- | --- | --- |
| world built | 4.7 s | 4.5 s (`ready` mark 4517 ms) |
| title, tab in front | 42–55 s | **44.3 s** |
| title, tab in background | never (still going at 420 s) | **7.5 s** |

Shaders are the same: 68 variants in the warm-up, 76–78 in the pre-roll. The foreground
number does not move until the menu is allowed up before those frames (CU-2).
