# cursor — CU-30 the favicon (part 1 of 2) — 2026-09-25
Changed: `index.html` names an empty inline icon, so the browser stops asking for `/favicon.ico`. The two 404s on every load are gone (CU-A7).
Files: index.html (head)
Tests: headless load with network logging → failed requests: none, page errors: 0.
Screenshots: none. Nothing visible changed.
Not verified: title times and the title menu's frame rate on Jerry's GPU (the other half of CU-30). That needs a visible browser, and Jerry is at the machine until 23:00 local.
Requests: none.
Contract changes: none.
