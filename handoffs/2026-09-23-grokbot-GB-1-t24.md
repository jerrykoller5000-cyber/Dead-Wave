# Grokbot GB-1 — t24 mine arming / combat audio

**Date:** 2026-09-23  
**Agent:** grokbot  
**Task:** GB-1  
**Touched:** `tools/tests/t24.js` only

## What

Updated the harness so mines, turrets, NVG and zombie voices run on a live match.

## Fixes

1. **Callsign + prep + insertion wait** — mine trip / turret tick need `updateTurrets` on a started match.
2. **skipGrace** after mine spawn and swarm.
3. **Chase voices** — swarm starts at ~50 m and voices only fire under 30 m while moving. After swarm, pull one of each type in to ~8–11 m with `riseT = 0`, then poll until `kinds >= 5` and `states` has `chase`.

## Result

`npm test -- t24 --jobs 1` → **14 pass, 0 fail**.

## Next

GB-1 t25.
