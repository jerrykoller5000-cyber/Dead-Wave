# claude — CL-60 Jerry's notes onto the board — 2026-09-26
Changed: Jerry's notes in `review/<asset>/notes.md` now reach the board. `node crew/crew.mjs review` lists every review folder with its version, owner and where the notes stand (no notes yet / Jerry's note waiting / owner is on it / answered: Jerry to look / approved), and writes a `notes.md` stub where there isn't one (the how-to and the example sit in a comment, so the parser ignores them). `review take <asset>` turns Jerry's waiting note into a request for the owner from `meta.json` (with the clip path, the render command and the answer command) and writes `> owner · taken · date` under the note. `review answer <asset> <agent> "<what changed>"` writes `> agent · vN · date: ...` under the note, with vN from `latest.txt`; it refuses while `latest` is still the version Jerry wrote about (`--force` to answer without one). Every crew panel prints `✎ Jerry's notes waiting` and `◉ For Jerry to look at`; `crew/panel.html` has a Review folders box and a "Look at <asset> vN" line under Your move with an Open button. The parser is `crew/notes.mjs`, shared by crew.mjs and the panel (dynamic import), so both read a note the same way. The panel can't list folders over http, so crew.mjs keeps `crew/reviews.json` (the folder list, no times, only rewritten when it changes). docs/studio.md §5 has the round trip. Notes stubs made for the five guardian folders AG-19 rendered.
Also, as lead: reviewed CU-44 and AG-19 (log). The renderer works, but its strip is one oblique scene with the creature a few pixels tall, so neither Jerry nor an agent can judge a pose from it; that's CU-45 on Cursor's queue (tiles, one side camera framed on the rig, the reference in its own row at human scale, meta.task, no identical re-renders: `review/guardian-gallop/v2` is the same clip as v1). CL-61 now waits on CU-45.
Files: crew/crew.mjs, crew/notes.mjs, crew/notes.test.mjs, crew/panel.html, crew/reviews.json, docs/studio.md, review/*/notes.md (stubs), crew/BOARD.md (CU-45, waits, mission row), qa/shots/2026-09-26-CL-60/panel-review-box.png
Tests: `node --test crew/notes.test.mjs` 4/4 (stub is empty; waiting → taken → look → approved; a new note on the answered version is waiting again; stray headings ignored). `node --import ./studio/node-three.mjs --test "studio/*.test.mjs"` 8/8. A full round trip in a scratch copy: note → review (waiting) → take (request + taken line) → answer refused at v1 → render v2 → answer (look at v2) → Jerry's "Good." (approved at v2). Panel rendered in headless Chrome with a waiting and an answered folder: qa/shots/2026-09-26-CL-60/panel-review-box.png. npm test not run (no game change).
Screenshots: qa/shots/2026-09-26-CL-60/panel-review-box.png
Not verified: the panel on Jerry's PC (served by Open Crew Panel.bat; the box appears once crew.mjs has written reviews.json, which any crew command does).
Requests: CU-45 (board) for Cursor: the strip layout.
Contract changes: none
