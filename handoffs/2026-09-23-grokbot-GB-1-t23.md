# Grokbot GB-1 — t23 chainsaw idle / reload cues

**Date:** 2026-09-23  
**Agent:** grokbot  
**Task:** GB-1  
**Touched:** `tools/tests/t23.js` only

## What

Updated the harness so gear sounds and reloads run on a live match.

## Fixes

1. **Callsign + prep + insertion wait** — `updateGearSounds` only starts the engine when `gameStarted`.
2. **Poll for idle loop** — pull-start is ~0.62 s before `sawLoop` exists; wait until `isChainsawRunning()` instead of a fixed 1.2 s.

## Result

`npm test -- t23 --jobs 1` → **8 pass, 0 fail**.

## Next

GB-1 t24.
