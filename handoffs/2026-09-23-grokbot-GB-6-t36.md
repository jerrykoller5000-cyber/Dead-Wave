# GB-6 t36 — burial death cine

## Result
**22 pass, 0 fail**

## Touch
- `tools/tests/t36.js` only

## Changes
Cine setup (grave mesh, diggers, cams) only runs after `CINE.black` (~2.6s). The old fixed waits hit `c.rec.g.position` before setup.
1. Wait for prep after Enter-starts-Play.
2. Stick the marine so damage/spawn are live.
3. Poll until `c.setup && c.rec.g`, then dig pats / walk-off / camB / letters / win panel.
4. Kept all original coverage (audio spies, shovel hit, play-again, filled grave).

## Next
GB-6 t37
