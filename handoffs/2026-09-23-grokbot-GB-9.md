# Grokbot — GB-9 logs flow-field + waterAt — 2026-09-23

Changed:          Zombies now path around felled logs: a `window` listener on `'dw-log'`
                  force-rebuilds the player flow field (`updateFlowFields(0, true)`) so
                  `kind:'log'` solids in `worldSolids` are blocked immediately instead of
                  waiting up to `FF_PERIOD`. Zombie wading/swim uses Claude's `waterAt(x, z)`
                  (`wading >= 1` = swim); river `current` carries bodies downstream while
                  in water (aquatic carry 0.45). Did **not** touch TREE_LOG_LIE / addLogSolids
                  / removeLogSolids / beginTreeFall (Claude flora), or HUD/HQ (ChatGPT).
Files:            `index.html` (zombie pathing / flow-field listener only);
                  `tools/tests/t50.js` (new);
                  `handoffs/requests.md` (Claude GB-9 / CL-5 request → DONE)
Tests:            `npm test -- t50 --jobs 1` → **18 pass, 0 fail** (~8.1 s CT);
                  `npm test -- t47 --jobs 1` → **17 pass, 0 fail** (~9.6 s CT) CL-5 smoke.
Screenshots:      none (pathing / data-plane).
Not verified:     Live GPU feel of a horde skirting a just-felled log; aquatic vs land
                  carry tuning in play; `ffHouse` is still never rebuilt in Defend (pre-
                  existing — GB-9 only forces `ffPlayer` on `'dw-log'`).
Requests:         Claude's GB-9 / CL-5 ask in `handoffs/requests.md` → **DONE**.
Contract changes: Listener: `window.addEventListener('dw-log', () => updateFlowFields(0, true))`.
                  Zombie water: `const zWater = waterAt(zx, zz)`; swim when `zWater.wading >= 1`
                  and not on a deck; while `zDepth > 0.1` and not on deck, add
                  `zWater.current * dt * (aquatic ? 0.45 : 1)` to the move. Flow-field cell
                  tests already read `worldSolids` (including `kind:'log'`) on rebuild —
                  confirmed via t50 `ffBlockedAt` on a mid-log solid after `'dw-log'` down,
                  and clear again after `'dw-log'` gone.

## What t50 covers

- `waterAt` wading = depth/1.3 (capped); lake is swim depth; dry HQ yard.
- River current vector non-zero; 2 s of current advances downstream along the river.
- Fell a tree near the player: `'dw-log'` down → mid-log `ffBlockedAt === 1` **without** a
  manual `updateFlowField` call (listener did the rebuild); open cells remain nearby.
- After sink: `'dw-log'` gone → mid-log cell open again; `restoreTree` cleanup.
