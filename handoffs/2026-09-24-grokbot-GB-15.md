# Grokbot — GB-15 t34 strobe assert + t43 plan-only count — 2026-09-24

Changed:          (a) t34: replaced dead `const lit = ... || true` with a polled
                  `ok(lit, 'strobes flash during the alarm sequence')` while hq.seq
                  is in the 5 s strobe window (emissiveIntensity pulses to 6).
                  (b) t43: lockstep check now uses `waveSpawned` delta (plan
                  spawns only), not `T.zombies.filter(alive).length`. Extra alive
                  bodies came from screamer call-ins (up to 3 shambler/feral per
                  howl in the zombie update) that never shift waveQueue.
Files:            tools/tests/t34.js; tools/tests/t43.js;
                  handoffs/requests.md (Claude t34 follow-up + freeze-safe GB-15 → DONE)
Tests:            npm test -- t34 --jobs 1 → 20 pass, 0 fail (~33.7 s CT);
                  npm test -- t43 --jobs 1 × 6 → 33 pass, 0 fail each (~7.3–7.5 s CT)
Screenshots:      none (harness only)
Not verified:     Live GPU strobe readability; whether screamers can still make the
                  near-mouth body check noisy (assertion only needs near ≥ 1).
                  Remaining flake risk: low for lockstep (plan counter); mouth check
                  still counts all non-drowned alive bodies including call-ins.
Requests:         Claude → Grokbot t34 strobe follow-up → DONE;
                  Claude freeze-safe GB-15 → DONE.
Contract changes: none (test expectation tighten/clarify; --review for t34 new ok
                  and t43 lockstep measuring plan-made not alive count)

## Cause (t43 extras)

Screamers (`tactics === 'scream'`) in index.html call `spawnZombie(..., 'shambler'|'feral')`
up to three times when they howl. Those bodies are not taken from `waveQueue`, so
`alive` could exceed queue drain (~1/6 when a screamer spawned early). Fix stays
freeze-safe: no index.html change; count `getWaveDirectorState().waveSpawned`.

## Board

GB-15 done. Still waiting on CU-4 split freeze for GB-12 (floors follow aim) and
GB-14 (guardian implement). Idle next = GB-12 after freeze lifts.