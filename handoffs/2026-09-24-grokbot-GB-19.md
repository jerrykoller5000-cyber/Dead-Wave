# grokbot — GB-19 getGuardianAlive() for boss pip — 2026-09-24

Changed:          Added `getGuardianAlive()` for ChatGPT's minimap boss pip: `null` unless a guardian night is planned and a guardian is alive; else `{ x, z, hp, hpMax, caveIndex }` from the live mesh. Exported on `TT`. Told ChatGPT the shape.

Files:            index.html (zombies), tools/tests/t53.js

Tests:            npm test -- t53 --jobs 1 → 76 pass, 0 fail (was 68; +export/null/alive/shape/after-death checks)

Screenshots:      none (API only)

Not verified:     Live minimap pip rendering (ChatGPT). Claude contract entry for getGuardianAlive not yet recorded.

Requests:         → ChatGPT: shape for boss pip

Contract changes: propose `getGuardianAlive()` → `{ x, z, hp, hpMax, caveIndex } | null` (Claude to record after review)
