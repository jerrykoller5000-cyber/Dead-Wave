# GB-1 t6 — drag builds (wall / platform / railing / scrap / floor)

## Result
`npm test -- t6` → **18 pass, 0 fail** (via dump runner; same file).

## Touch
- `tools/tests/t6.js` only (Cursor still owns index.html parts)

## Changes
1. Live-match harness: callsign, wait for prep, stick far from porch so the loop runs.
2. Wall lookups via edge slots (`edge0`–`edge3`), not `base`.
3. Hub-arms assertion flipped: middle piece is a full-edge wall with empty/unused `userData.arms`.
4. Platforms / railings read deck at **level 1** (`cellOccupant(..., 1, 'base')`).
5. Scrap banner: `setScrapHeld(true)` before `updateGhostPreview` so `updateScrapMark` fills the hint.
6. Floor wall-to-wall: while grounded, drag prefers boardwalks; if drag plan is not all-green roofs, place with `{ forceLv: 1 }` (same rule as t5/t11/t12). Coverage kept (5 floors across the gap).

## Next
GB-1 t7 (or next failing combat thrower).
