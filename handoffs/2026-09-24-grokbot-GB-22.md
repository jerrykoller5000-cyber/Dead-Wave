# grokbot — GB-22 Take out the death replay (D-20) — 2026-09-24
Changed:          Removed the combat side of scripted-death replays per D-20 / Jerry. Live cave and pit deaths unchanged. Gone: the four helpers (`listScriptedDeathReplays`, `canReplayScriptedDeath`, `beginScriptedDeathReplay`, `isScriptedDeathReplay`), the `scripted-death-replay` event, the replay branch in begin/finish/abort/updateScriptedKill, and the death-screen mount of `mountDeathReplays` (so endGame does not call removed helpers). t56 retired with a header reason.
Files:            index.html (scripted deaths), tools/tests/t56.js
Tests:            npm test -- t56 t36 t37 --jobs 1 → 39 pass, 0 fail (t56 1/0 retired; t36 22/0; t37 16/0)
Screenshots:      none (API removal; live cines covered by t36/t37)
Not verified:     ChatGPT GP-14 UI teardown (`ui/replays.js`, Watch again button, catalogue tiles, `ui/replays.css`, replay strings). `docs/contracts.md` D-18 section still lists the helpers — Claude should withdraw it.
Requests:         → ChatGPT: combat helpers gone; GP-14 can remove Watch again / catalogue callers and ui/replays.* safely. → Claude: withdraw GB-20 / D-18 replay contracts (D-20).
Contract changes: removed (D-20 withdraws D-18): listScriptedDeathReplays, canReplayScriptedDeath, beginScriptedDeathReplay, isScriptedDeathReplay, dw-game scripted-death-replay — needs Claude to strike from docs/contracts.md
