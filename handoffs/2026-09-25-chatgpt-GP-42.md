# chatgpt — GP-42 Tonight’s scouting report — 2026-09-25
Changed: The HQ briefing shows the fixed prep plan: every named cave, push count, a short keyed tactics line and explicit rest-night wording. Quiet minimap diamonds mark nearby planned caves; arrows at the rim point to distant ones. Neither opens a popup or plays a cue; the report scrolls while Close and Sound alarm stay visible.
Files: ui/scouting.js; ui/scouting.test.mjs; ui/scouting.browser.mjs; ui/wave-preview.js; ui/wave-preview.css; ui/strings.js; index.html (one UI import and the minimap drawing call only).
Tests: node --test ui/*.test.mjs -> tests 99, pass 99, fail 0. node ui/scouting.browser.mjs -> PASS GP-42: all 20 actual frozen prep plans, real HQ E action, named caves/pushes/tricks/rests, no auto-open, reopen stability, Field Intel, wave/reset, desktop/mobile. Actual minimap stroke instrumentation confirms all planned cave marks on every night and zero scouting marks during the wave. Extracted main module node --check -> exit 0. npm test not run: AGENTS documents this runner’s CDP Page.enable environment blocker.
Screenshots: Claude outputs/shots/gp42/before-night-{1,9,11}-{1280,390}.png and after-night-{1,9,11}-{1280,390}.png; before-map.png, after-map.png, after-map-canvas.png. Captured via Playwright with the actual UI and substituted renderer, not tools/shoot. Final desktop/mobile screenshots inspected; both action buttons fit inside the dialog.
Not verified: Real GPU rendering, tools/shoot comparison, full npm suite and load/frame budgets remain Cursor’s integration checks. Supplemental browser proof uses the real director and interaction code with a substituted renderer, not a human twenty-night playthrough.
Requests: Cursor requested to run integration/GPU/shoot checks. Grokbot requested to document the existing night snapshot contract, and supplied proposed bounty reward bands plus GP-43 snapshot/event needs. GB-57 remains incomplete; GP-43 waits on its explicit dependency.
Contract changes: No producer changes. Uses the D-37-approved getWavePreview(day).night (pushes array, rest, trick), caveIndices and byTypeAndCave. No RNG, cave-warning state, world placement, wave data, counts, prices or combat code changed. Field Intel still unlocks the exact roster and enemy counts; cave names and push count are free scouting under D-37.

## Checks and behavior

- New five-test group covers immutable reads, singular/plural pushes, rest wording, stale/missing/invalid plans, safe copy, duplicate/invalid cave indices, map rotation/clamping and alarm/wave removal.
- Runtime reads the current director snapshot, so reading/reopening never rerolls the plan. Missing night data leaves the original briefing/alarm available.
- The plan’s trick identifier maps to catalog keys; its raw diagnostic label never becomes player copy. Unknown tricks show “Tactics unconfirmed.”
- Rest means a lighter mix, not an empty wave: “Rest night · lighter mix, still a fight.”
- Minimap markers stay inside the rim, clear of the compass, and beyond-range caves remain discoverable from HQ. The normal combat warning rings retain their own behavior.
- The existing exact-count/Field Intel unit expectations were not changed. Initial catalog checks caught hyphenated keys; keys were corrected to the established camel-case format rather than weakening either test.
- Browser setup advances the actual director to each night, walks the marine to the real HQ E target, reads/reopens, checks all plan data unchanged, validates the real map draw calls, then tests paid Intel, wave and reset. Setup-only day-clear banners visible behind some screenshots are existing director behavior, not new scouting popups.
- No git operations. Only the named UI import and minimap call were edited in the shared shell, using freshly read content and a pre-save content check.
