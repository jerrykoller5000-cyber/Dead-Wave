# cursor — CU-46 Scene renderer — 2026-09-26
Changed: `node tools/studio.mjs scene <scene.json>` draws every actor (the marine is the marine rig) into `review/<scene>/vN/`. Side-on tiles follow the bodies, a top-down row shows the path and where each one is, and the video is real speed then quarter speed, from the side and then the game camera. A tile is red where `studio/scene.js` flags a check, and `stats.json` keeps the worst of each. The same scene bytes do not grow a version.
Files: tools/studio.mjs, tools/studio-scene.html, review/demo-drag/
Tests: npm test not run. `node tools/studio.mjs scene studio/scenes/demo-drag.json` → v1 in 67.2s. A second run: unchanged, no v2. Worst checks match the player: gap 0.92 m when the hand first reaches, guardian feet sliding 2.26 m, a 0.36 rad snap.
Screenshots: review/demo-drag/v1/strip.png, review/demo-drag/v1/video.webm
Not verified: on Jerry's GPU. Headless software frames are slower than realtime, so the 30 s of video (a 3 s scene, four passes) took about a minute.
Requests: none new.
Contract changes: none
