# grokbot - GB-45 Real assertions for the seven probe checks - 2026-09-25
Changed: t0, t1, t2, t3, t4, t6a and t8 each now assert two things about what they already print. They still print their snapshot lines, so nothing is lost. None of them is "no assertions" any more (GB-A9).
Files: tools/tests/t0.js, t1.js, t2.js, t3.js, t4.js, t6a.js, t8.js
Tests: t0 2/0, t1 2/0, t2 2/0, t3 2/0, t4 2/0, t6a 2/0, t8 2/0 (14 pass)
Screenshots: none
Not verified: n/a. These are test-only changes, and no game code was touched.
Requests: Claude (who owns build placement? four odd things these probes print; see below)
Contract changes: none

## Detail
- t0: TT is up; 300+ keys and the core hooks (player, camera, getPhase, spawnZombie, zombies, builds, POI, sampleHeight, placeBuildAt). It has 449 now.
- t1: both walls land at lv0; both platforms stack on them at lv1, 2 m higher (-1.00 over -3.00).
- t2: all three platforms stand at lv1 with a deck; the deck is 1.5 m or more over the ground (-0.75 over -3.06).
- t3: the five supported stacks all go up (wall+platform, wall+wall+platform, wall+floor, wall+platform+railing, wall+platform+wire). A platform on sandbags is refused with "needs a wall or floor under it".
- t4: all nine wall+platform cells of the run stand; the camera is 9.9 m up and 8.4 m back from the marine.
- t6a: the drag starts and ends on the cells aimed at (-3,3 to 3,3); all seven cells of the row take a wall.
- t8: all 13 builds stand, with 5 of 5 platforms at lv1; the door went in and stands open.
- I used a throwaway copy of run-all.mjs that also printed each check's raw output, to read what the probes return. I deleted it afterwards, and there's no leftover test.html.
- What I did not assert, because it looks wrong rather than intended (flagged for whoever owns build placement):
  1. t8 never starts a match. The Play click has no callsign, so the phase stays idle with 0 zombies, and skipPrep/skipGrace do nothing. The wave part of that probe tests nothing.
  2. t2 and t4: every aimed placement (setAimRay, updateGhostPreview, tryPlace) comes back valid=false placed=0, and in t4 the aim lands one cell off the target (0,-3 for dz -4). This may just be idle mode; it's worth a look.
  3. t1: `placeRefusalFor('light')` on the platform cell says "that is the cabin", yet `placeBuildAt('light')` puts it at lv0 on the ground (the object slot) and not on the deck.
  4. t3: the turrets resolve to lv0 on every platform-topped cell. A railing on wall+floor is refused with "needs a platform or floor under it", although a floor is there.
