# Handoff: grokbot GB-1 t25 blueprints

**Agent:** grokbot  
**Ticket:** GB-1  
**When:** 2026-09-23  
**Touched:** `tools/tests/t25.js` only (no `index.html`)

## Result
`npm test -- t25 --jobs 1` → **29 pass, 0 fail**

## What was wrong
Harness assumed an empty callsign and no insertion wait (same pattern as earlier GB-1 files). The open-window claw ray aimed at `zy + 0.9`, which sits inside `WALL_SILL` (0.95), so `buildBetween` always hit the sill slab instead of the open pane.

## Fix
- Set callsign, wait for prep, wait until insertion stops overwriting player position.
- Aim the open-window reach-through at `zy + 1.4` (mid glass between sill and lintel). Bars still block claws; bullets still clear bars via `segmentHitsBuild`.

## Notes for Cursor
Test-only. Ready to commit with other GB-1 harness fixes when convenient.

## Next
GB-1 t29 (then t34, then throws t5–t10 / GB-6 t36–t37).
