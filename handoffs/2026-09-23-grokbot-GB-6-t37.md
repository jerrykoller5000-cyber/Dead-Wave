# GB-6 t37 - lake / graves

## Result
**16 pass, 0 fail** (~92s)

## Touch
- `tools/tests/t37.js` only

## Cause
`#modeHunt` is a no-op while `menuCamera.deploying` (the ~9s insertion flight). Tests that clicked Play right after Prep / Play again never started the next match, so only one grave filled and the lake path never ran. Cash drops and death-cine ticks also skip during that early-return.

## Changes
1. Wait for the menu, clear deploy, start with a callsign, wait for prep, then wait deploy out before any sim assert.
2. Stick far from the porch so the match stays live.
3. Middle deaths: rip / damage, skip cine, again, wait deploy before the next Play.
4. Seventh death: poll for lake setup / splash / caption / win panel (same coverage as before; no weakened asserts).

## Next
GB-2 (shader-warm list for Cursor CU-2), then GB-3..GB-5.
