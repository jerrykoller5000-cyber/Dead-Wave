# grokbot — GB-23 Pistol own .45 ammo — 2026-09-24
Changed:          Split the pistol off the shared 9mm pile onto its own .45 calibre. Uzi stays on 9mm. New pack `.45` = 36 rounds / $12, reserve cap 120; starter reserve is 36×.45 (9mm starts at 0 until the Uzi is owned). Exported `caliberOf` on TT for probes. Updated t54 starter-pack assertion; added t57.
Files:            index.html (weapons ammo), tools/tests/t57.js, tools/tests/t54.js
Tests:            npm test -- t57 t54 --jobs 1 → 42 pass, 0 fail (t57 10/0, t54 32/0)
Screenshots:      none (ammo tables / kiosk data)
Not verified:     ChatGPT string keys and prep CALIBRES map for `.45`; live kiosk label still shows raw `.45` until strings land. Hardcoded SHOP_HINT in index still says pistol+Uzi share 9mm (ChatGPT shop copy).
Requests:         → ChatGPT: ids and strings for kiosk / prep (below).
Contract changes: ammo calibre `.45` for pistol; grant id `ammo:.45`; pack `{ n: 36, cost: 12 }`, cap 120. Claude: note in contracts if ammo calibres are listed there.

ChatGPT ids:
- calibre key: `.45`
- grantSupply / pack id: `ammo:.45`
- string key to add: `calibre.45` → ".45"
- update `shop.hint.ammo` (and index SHOP_HINT.ammo if still used): pistol no longer shares 9mm with the Uzi
- prep-checklist.js CALIBRES: add `'.45': '45'`
