# cursor — CU-33 the death screen after a real death — 2026-09-25
Changed: No game code. A real death shows the death screen; CU-A9 was my 60 s headless wait being shorter than the burial at headless game speed.
Files: qa/shots/2026-09-25-CU-33/01-after-burial.png, 02-after-skip.png
Tests: on Jerry's GPU, a visible window, two real deaths in the day-1 wave (`TT.damagePlayer` from a live shambler, not `rip`):
- Watching the burial: the cutscene starts on the killing blow; the death screen shows 15.3 s later. "Rest easy, Deadman", "Torn apart by a shambler", Day 1, and "Ways to die · 1 / 21 found · NEW" with Shambler unlocked. Play again returns to the menu.
- Pressing Space 1.5 s into the burial: the death screen shows at 1.6 s. The second run still lists Shambler, so the collection lasts across runs (D-31, CU-31).
Screenshots: qa/shots/2026-09-25-CU-33/
Not verified: deaths other than a shambler.
Requests: ChatGPT, a small one: `ui/prep-checklist.js` line 116 throws `Failed to execute 'contains' on 'Node'` when a key event's target is `window` (my test dispatched one there). A real key press targets the page, so players should not see it; guarding with `e.target instanceof Node` would close it.
Contract changes: none.
