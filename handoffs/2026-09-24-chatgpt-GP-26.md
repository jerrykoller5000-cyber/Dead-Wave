# chatgpt — GP-26 Browser timing checks — 2026-09-24
Changed: Replaced fixed delays with bounded polling for real landing, pickup, jab, crate opening/fade/removal, HUD presentation and objective states. Objective restart waits for the core's cancellation acknowledgement as well as the UI reset. No production game behavior changed.
Files: tools/tests/t35.js; ui/hud-phase1.browser.mjs; ui/objectives-live.browser.mjs; new ui/browser-poll.mjs and ui/timing.browser.mjs.
Tests: `node --test ui/*.test.mjs` → 73 pass / 0 fail. Targeted serial run: HUD PASS, t35 28 pass / 0 fail; objective initially timed out after damage because its UI reset preceded core cancellation. After polling the core acknowledgement, `node ui/objectives-live.browser.mjs` → PASS all seven sites, hold/release/damage, focused movement/E, reward cues and reset. Final `node ui/timing.browser.mjs --jobs 3` → `PASS GP-26 targeted runner: 3 checks with --jobs 3. Shared npm test is separate.` HUD/Ember, objective integration and t35 all passed concurrently; all 28 supply assertions retained, no page errors. NODE_PATH uses the existing bundled Playwright runtime; Edge is headless, renderer a stand-in. No dependencies installed.
Screenshots: Existing headless HUD/objective integration shots refreshed under Claude outputs/shots/gp23 and gp11-live. No visible production change in this task; no new before/after comparison required for test-only edits.
Not verified: Full `npm test -- --jobs 3` not run here because of the documented shared CDP Page.enable limitation; Cursor must verify it at integration. This targeted runner does not prove the entire shared suite is green. No real GPU/performance claim.
Requests: Cursor final shared-suite jobs-3 verification and commit; shared startMatch in tools/tests/lib.js still has a fixed 10-second landing wait, and other owners' t34 etc. were left alone. GP-27 hook is live for Cursor's screenshot/bench/profile caller migration.
Contract changes: None. Test expectations were not relaxed; outcome predicates and bounded deadlines replace wall-time guesses. Virtual-clock timing in the isolated opening test is deliberate, not a live-animation sleep.

Audit: UI browser scripts now have no waitForTimeout calls. Existing coach/briefing,
kiosk and guardian-reward scripts already poll relevant results. In t35, remaining
wait calls are polling cadence only (25ms in until, 100ms observing freefall/chute/
landing), not fixed delays before assertions. Supply landing has a bounded 45s
deadline to permit slower simulated frames under concurrent load.
