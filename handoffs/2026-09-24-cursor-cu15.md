# cursor — CU-15 FPS counter and megaswarm — 2026-09-24
Changed: The FPS overlay now uses real frame times: average over the last second, the 1% low, the worst frame, and a count of frames over 50 ms. The debug console command `megaswarm` spawns 500 shamblers around (0, 18) with seed 15, past the normal cap, via `spawnZombie(..., true, true)`. `tools/bench.mjs` runs that swarm and prints the four numbers.
Files: index.html (perf), tools/bench.mjs
Tests: `node tools/bench.mjs --seconds 3 --count 8 --headless` spawned 8 and printed fps 0.3, low 0.3, worst 2997.6 ms, hitch 2. That is the headless timer, not Jerry's GPU. `t55` was not re-run. Full npm test not re-run.
Screenshots: none.
Not verified: 500 zombies for 30 s on the real GPU. That is Antigravity's AG-9: `node tools/bench.mjs`.
Requests: Grokbot already answered: `spawnZombie` with overCap. No new combat export.
Contract changes: none.
