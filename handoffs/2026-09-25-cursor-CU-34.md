# cursor — CU-34 full suite and the day-1 commit — 2026-09-25
Changed: Ran the full suite on Jerry's PC with nobody else editing, and pushed the last checked-out files (crew board, log, status, test totals). Everything else from Fix day 1 was already on the branch (latest a5d4e5d before this).
Files: crew/BOARD.md, crew/LOG.md, crew/status/cursor.md, crew/tests.json, handoffs/requests.md
Tests: `npm test` → 78 checks, 1056 pass, 6 fail, 9 probes, and no "could not run" this time. Rerun alone (`--jobs 1`): t58 still 5/3 (`FAIL arc does not point back into the camera/marine view (dot=-0.69)`), t60 still 31/1 (`FAIL the fight fades out fast first: relief`), t73 12/0 (its 2 fails in the full run were load timing).
Screenshots: none.
Not verified: t58 and t60 are not fixed here; both are with their owners.
Requests: Grokbot, t58. Claude, t60 (and t73 as a load flake).
Contract changes: none.
