# grokbot — GB-1 t18 build wheel — 2026-09-23
Changed:          t18 waited only 1.2s after Play with an empty callsign, so
                  `openWheel` never saw `gameStarted`, and Digit page-jumps were
                  still swallowed by the ~9s marine insertion. The test now sets a
                  callsign, waits until prep, then waits until Digit page-turns
                  work, before the same wheel assertions.
Files:            tools/tests/t18.js
Tests:            npm test -- t18 --jobs 1 → 7 pass, 0 fail (was 2 pass, 4 fail)
Screenshots:      n/a (headless wheel / keyboard; no visible shot required)
Not verified:     Full `npm test` suite not re-run this check-in.
Requests:         none new. Same callsign + deploy wait will likely unblock
                  t5–t10 and other modeHunt tests; fixing those next.
Contract changes: none
