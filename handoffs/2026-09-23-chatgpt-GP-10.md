# ChatGPT — GP-10 Guardian-night strings — 2026-09-23
Changed: Added seven keyed messages for the fightable Guardian, its briefing warning/source and the approved first-blood reward. The reward copy says skull value and directs the player to bank it at the HQ window.
Files: ui/strings.js; own crew card and handoff/request notes.
Tests: node --test ui/*.test.mjs: 51 pass, 0 fail. Existing catalogue tests validate formatting of every new key and duplicate-key absence; no test expectations changed. npm test not run under the AGENTS.md CDP Page.enable exception; Cursor runs the shared suite before commit.
Screenshots: Not applicable: keys only, no new visible caller or layout. No index.html edit during the split freeze.
Not verified: Guardian gameplay, reward delivery/save receipt, warning visibility and performance await GB-14 integration; this task adds text only.
Requests: Grokbot: use the canonical keys below in the post-split integration instead of the spec's provisional wave.preview.* names. Cursor: include the checked-out catalogue addition at commit time.
Contract changes: None. Existing text(key, params) API unchanged; no combat/economy wiring.

## Keys and proof

```text
node --test ui/*.test.mjs
tests 51
pass 51
fail 0
```

Directly rendered every new key with `text(key, {source:'Chalk cave'})` using
`node --input-type=module -e`:

```text
enemy.guardian.name: Guardian
wavePreview.guardianNight: Guardian night
wavePreview.guardianFromCave: From Chalk cave
wavePreview.guardianUrgent: A Guardian is coming from Chalk cave.
reward.guardianFirstBlood: First Guardian defeated
reward.guardianBlueprint: Mortar blueprint unlocked
reward.guardianSkulls: +80 skull value — bank it at the HQ window
```

Use the actual keyed source name in `{source}`; the example above is not a fixed
cave label. `enemy.guardian.name` is the roster label corresponding to the spec's
provisional `wave.preview.hasGuardian`. The three briefing keys use the existing
`wavePreview` namespace, as settled in the guardian review. No duplicate aliases.
`enemy.caveguard.name` remains distinct and unchanged for the immortal scripted enemy.

The urgent Guardian/source warning must remain visible without Field Intel when
wired. Full counts remain paid, and static preview must not invent an HP value.
Reward text is for the confirmed economy result only: free mortar blueprint if
not already owned, otherwise the fixed 80 bonus skull value for collection and
banking. No direct Cash, placed mortar, ammo or paid-purchase event is implied.
See handoffs/2026-09-23-chatgpt-guardian-review.md for the approved economy decision.
