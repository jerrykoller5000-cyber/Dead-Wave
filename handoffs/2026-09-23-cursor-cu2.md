# cursor — CU-2 Fast title — 2026-09-23
Changed: The menu opens on the first animation tick, once the world is up. The first-minute set (shambler, feral, pistol casing, tracer, muzzle, grenade, knife, and generic / melee / explosive kills, plus dust, splash, ripple, cash and a med drop) then warms one stage per tick behind the menu. The full staged fight and the live pre-roll are unchanged and still run after that; the live fight is drawn and then hidden so the menu camera is the frame that shows. The hidden-tab pump logs the first thrown tick and stops only after 30 throws in a row. `DWLoad`'s warmup step ends when that first-minute set has drawn, not when the whole roster has.
Files: index.html (boot)
Tests: full `npm test` not re-run. `node tools/tests/run-all.mjs t45 --jobs 1` → 8 pass, 0 fail, 7.7 s. Load times from `node tools/loadtime.mjs`: foreground warm, title at 6.8 s (world `ready` mark 4.6 s, `window.TT` at 6.5 s); was 42–55 s. Background warm, title at 10.8 s (`TT` at 6.4 s); was never, past 420 s. A background run before the idle gap measured 15.4 s because the staged fight blocked the page and the throttled dissolve timer could not be observed. First-minute shader log from that earlier background run: 68 variants when the menu opened, 74 after the set (`a-fresh` 69, `a-flash` 71, `a-kill-generic` 73, `a-kill-explosive` 74).
Screenshots: none. This is the load gate, not a scene change.
Not verified: cold-cache load; a full `npm test`; the live pre-roll's final variant count (the page was closed once the title showed); GPU memory while a hidden tab is pumping. The 5 s warm budget is still missed: in front the menu is about 0.3 s after the world, and the world itself reaches a first frame at about 6.5 s wall time. The bake that would cut that is parked (D-2).
Requests: none.
Contract changes: none. `ready` on the load channel now fires when the first-minute set is warm, which is what the 2026-09-23 loader note already asked for.
Commit: index.html was not committed. Grokbot is in `index.html (build place)` and ChatGPT is in `index.html (HQ briefing shop HUD)`, and the working copy also holds their edits.
