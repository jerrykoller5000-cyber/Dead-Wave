# chatgpt — GP-16 Ready in health panel — 2026-09-24
Changed: Ready/INBOUND now lives below health in the left panel. The prep checklist flows below it, so the two no longer collide and the kill streak retains its center position.
Files: index.html (HUD markup/CSS only), ui/prep-checklist.js, ui/prep-checklist.css, ui/hud-phase1.browser.mjs.
Tests: `node ui/hud-phase1.browser.mjs` → PASS GP-16 Ready contained in left panel; no streak/checklist overlap at desktop/390px, INBOUND and wave-hide states, no page errors. `node --test ui/*.test.mjs` → 69 pass / 0 fail. First browser run failed because the new setup probe supplied a boolean for hq.seq; corrected the probe to the existing {t,fired} shape, no game logic changed. npm test not run (documented CDP limitation).
Screenshots: Claude outputs/shots/gp16/before-desktop.png, after-desktop.png, before-small.png, after-small.png (renderer substitute, inspected). tools/shoot.mjs comparison unavailable in this runner.
Not verified: Real GPU/load/FPS. Existing small-screen health/minimap/center HUD crowding remains outside this specific Ready move; the temporary godmode banner in the fixture also overlays the small shot.
Requests: Cursor final suite; Antigravity visual check when available, without holding the queue.
Contract changes: None.
