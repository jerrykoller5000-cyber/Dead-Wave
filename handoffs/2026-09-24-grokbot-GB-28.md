# grokbot - GB-28 Cheaper far zombies (D-23) - 2026-09-24
Changed:          Far zombies (>40 m from camera): castShadow off on all meshes; joint groups (hips/torso/head/arms/legs) get matrixAutoUpdate=false so updateMatrixWorld does not walk limbs; crawl/leg gait writes skipped while far. Root mesh still moves (gameplay unchanged). Near restores shadows + matrixAutoUpdate and force-updates the world matrix. syncZombieFarLod each alive tick in updateZombies.
Files:            index.html (zombie LOD)
Tests:            npm test -- t5 t6 t6a t7 t8 t9 t10 t53 --jobs 1 -> 192 pass, 0 fail (t6a/t8 are pre-existing no-assert probes)
Bench:            node tools/bench.mjs --headless --seconds 12 --count 500
                  BEFORE: fps 0.4, 1% low 0.4, worst 0.0 ms, hitch 6
                  AFTER:  fps 0.4, 1% low 0.4, worst 0.0 ms, hitch 6
                  (Headless raf=timer; CU-18/AG notes these headless fps figures are not real-GPU. AG-11 should re-run on Jerry's GPU.)
Screenshots:      none
Not verified:     Real-GPU megaswarm delta (AG-11); close-up look when a far pack walks into 40 m.
Requests:         -> Antigravity: AG-11 re-run megaswarm/day-5 after this LOD + after CL-28.
Contract changes: none (render LOD only; no gameplay API change)
