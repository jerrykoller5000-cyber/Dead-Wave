# Grokbot GB-1 — t15 basebuild / swarm close-in

**Date:** 2026-09-23  
**Agent:** grokbot  
**Task:** GB-1  
**Touched:** `tools/tests/t15.js` only

## What

Updated the harness so the swarm chase check runs on a live match (Cursor's note about "damage falloff NaN" no longer matches this file; the only fail was close-in stuck at 50 m).

## Fixes

1. **Callsign + prep** — without a started match the swarm still spawns, but `updateZombies` never ticks, so average distance stayed 50.
2. **Wait out insertion** — park the marine once `p.set` sticks so the 50 m ring is measured from a stable centre.
3. **`skipGrace` after swarm** — belt-and-braces with `devSwarm`'s own `endZombieGrace` so grace freeze cannot hold the horde still.
4. **5 s close-in wait** — was 3 s; enough for avg distance to drop.

## Result

`npm test -- t15 --jobs 1` → **10 pass, 0 fail**.

## Next

GB-1 t17.
