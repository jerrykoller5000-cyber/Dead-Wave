# Grokbot — GB-8 floor level from ground aim + CL-9 test restores — 2026-09-23

Changed:          **Game bug** (Claude suspected regression; confirmed). A grounded marine
                  aiming a floor at wall tops got a **boardwalk at his feet**, not a roof:
                  `resolveTarget` for floors ignored `opts.lv` / aim and preferred lv0, and
                  after the edge-wall move, cell-centre aim often missed the thin wall mesh
                  while `setAimRay` pins `aimTarget` to terrain. Fixed floor level resolution
                  and wall-top aim; restored real t6 assertions; unforced roof checks in
                  t5/t11/t12; explicit t12 "exactly one piece" check; corner-door marked
                  **intended** for edge walls. Did **not** implement ChatGPT D-8
                  `purchase-delivered` (left LATER after GB-8).
Files:            `index.html` (build place / floor level only: `resolveTarget` groundDeck,
                  `getPlacePoint` wall-top ray height, `beginPlaceClick` lv without hit,
                  `dragPlan` far-wall green + opts pass-through, `commitBuildDrag` multi-pass);
                  `tools/tests/t5.js`, `t6.js`, `t11.js`, `t12.js`
Tests:            `npm test -- t5 --jobs 1` → **31 pass, 0 fail** (~18 s CT);
                  `t6` → **20 pass, 0 fail** (~31 s CT);
                  `t11` → **13 pass, 0 fail** (~8 s CT);
                  `t12` → **22 pass, 0 fail** (~27 s CT).
Screenshots:      none (headless placement / drag probes).
Not verified:     Live GPU feel of wall-top roof aim on steep slopes; intentional boardwalk
                  *inside* a walled room still needs `forceLv: 0` (elevated support wins);
                  D-8 `purchase-delivered` emits (deferred).
Requests:         none new. D-8 stays LATER (after this checkout).
Contract changes: Floor level order (no `forceLv`): honour elevated **aim** first (wall/edge
                  hit → storey above; ray height over walled cell ≥ wall-top → roof lv);
                  then any storey `structureTop` / `floorSpanDepth` can hold; then boardwalk.
                  Aiming at a **platform** never falls through to a boardwalk. Elevated aim
                  that cannot roof **refuses** (no quiet boardwalk). Floor **drag commit**
                  walks the line in passes so the far wall can catch mid-span cells.
                  `beginPlaceClick` stores `pt.lv` even when the thin wall was not hit.
                  Corner-door: **intended** — edge walls are plain panels (no hub), so a
                  corner-cell wall may take a door; second cut still refused.

## Floor-from-ground verdict

**Bug, fixed.** Evidence: with walls present, grounded `placeBuildAt('floor')` and wall-top
drag preferred lv0 boardwalks; t5/t6/t11/t12 had papered over that with `forceLv` / `ok(true)`.
After the fix, unforced roof-on-walls and wall-to-wall roof drag (all level 1) are green.

## t5 corner-door verdict

**Intended** for the edge-wall system. Hub-and-arm walls refused corner doors; edge walls
have no hub arms, so any uncut wall panel (including a corner cell) can take a door. Test
asserts that plus second-cut refusal.

## t6 / t12

- t6: real `ok(plan all green)`, five roofs at level 1, plus `forceLv:0` boardwalk sanity.
- t12: explicit `builds.length === nT + 1`; aimed placement from GB-7 kept; room roof and
  stairs floor unforced.