# grokbot - GB-40 Day 1 is 15, half from the ground; no spawns on a marine at the mouth (D-29, GB-A7) - 2026-09-25
Changed: day 1 is now 15 shamblers. 7 or 8 claw up out of the ground in the treeline 35-60 m from the HQ, out of the camera's view and never within 25 m of the marine; the rest walk out of the day's cave. Day-1 zombies get no cave role. With the marine within 8 m of the mouth, cave spawns come from deeper inside (lz -3.6..-3.1), and a burst is held if a spawn point would be within 2.5 m of him.
Files: index.html (waveComposition, startPrep plan and preview, spawnWaveBatch, new pointInCameraView / groundRiseSpotOk / pickGroundRiseSpot, TT exports), tools/tests/t70.js (new), tools/tests/t43.js, tools/tests/t48.js (both mine; expectations updated for D-29), docs/contracts.md (Wave preview section)
Tests: t70 21/0 (new), t43 35/0, t48 22/0, t53 76/0, t59 34/0, t69 17/0, t61 18/0, t62 7/0, t36 22/0, t5 31/0, t6 20/0, t7 16/0, t9 19/0, t10 30/0. t60 (Claude's) 30/2: its proximity step clears zombies mid-spawn and now meets the ground risers, see Requests.
Screenshots: none. Not visual beyond where they appear; AG-17's day-1 play-through will show it.
Not verified: how the risers read in real play on Jerry's GPU. The out-of-view test is a frustum test and ignores occlusion, so it errs on the hidden side.
Requests: Claude: t60 line 65 should call T.drainWavePlanDbg() before T.clearZombies(). ChatGPT: the briefing can key a ground label off byTypeAndCave bucket.ground (it shows as perimeter today).
Contract changes: wavePreview gains groundByIndex and groundRisers; the ground bucket has ground: true (caveIndex -1). Director caveByIndex uses -2 for a ground row. New TT exports: getWaveSpawnLog, pickGroundRiseSpot, pointInCameraView. Zombies that rise carry z.groundRise. Recorded in docs/contracts.md.

## Detail
- **Composition.**
  - `waveComposition(1)` returns 15 shamblers (`DAY1_TOTAL`) instead of the 20 from `swellWithFodder(['shambler','shambler'])`.
  - `startPrep` marks 7 or 8 rows as ground (`GROUND_RISE` = -2 in the working plan), spread evenly and starting with row 0, so the first bodies of the night are the close ones. This is the dead-minute fix.
  - This doesn't apply on guardian nights, which never fall on day 1 anyway.
- **Preview.**
  - `total` is 15, so the briefing and the HUD say 15.
  - Ground rows show as `caveIndex -1` so ChatGPT's validator (which rejects < -1) keeps working.
  - `groundByIndex` and `groundRisers` say which rows they are, and the bucket carries `ground: true`.
  - t43 now checks ground rows (caveIndex -1, shambler, count = groundRisers) and that day 1 is 15 with 7 or 8 from the ground.
- **Where they rise** (`pickGroundRiseSpot`):
  - It first tries up to 60 spots 1-2.5 m off live trees whose trunks are 32-63 m from the HQ, run through `resolveStaticProps`. If none passes, it tries 60 random spots in the 35-60 m ring.
  - A spot passes if it is 35-60 m from the HQ, at least 25 m from the marine, dry, at least 6 m from any cave, and outside the camera frustum at ground and head height.
  - If no spot passes, that body walks out of the day's cave instead. That can happen if he stands in the ring looking the right way.
  - They spawn without `noRise`, so they play the existing claw-up (dust, chunks, emerge sound).
- **No cave role on day 1:** `applyCaveRole` is skipped when `day === 1`. t48 now asserts that there are no traits on day 1, then moves to day 2 and checks that the stamp still works there.
- **GB-A7:**
  - With the marine within 8 m of the planned mouth, cave spawns use lz -3.6..-3.1 instead of -1.4..-0.2.
  - If a spawn point is still within 2.5 m of him, the burst holds for 0.35 s (the same backoff as a full field) and doesn't use up the queue entry.
  - t70 stands him 4 m out: the nearest spawn was at least 5 m away, with no grab and no chase.
- **t70 also checks** (from the spawn log at spawn time):
  - The preview adds up (15 total, 7 or 8 ground, one ground bucket).
  - All 15 spawn.
  - Risers are 35-60 m from the HQ, at least 25 m from the marine and not in view.
  - The rest came from the day's cave.
  - Risers have `riseDur > 0` and no cave.
  - No day-1 zombie has a trait.
