# Grokbot GB-1 — t13 roof see-through

**Date:** 2026-09-23  
**Agent:** grokbot  
**Task:** GB-1  
**Touched:** `tools/tests/t13.js` only

## What

Updated the harness so cutaway and floor-level checks run after a live match and insertion.

## Fixes

1. **Callsign + prep** — Play needs `#playerName`; wait until `getPhase() === 'prep'`.
2. **Wait out insertion (~9s)** — until `player.position` sticks after `p.set`; cutaway only runs once the match is live.
3. **Roof placement** — `{ forceLv: 1 }` so the deck sits on the walls while the marine is on the ground (plain place boardwalks at 0).
4. **Cutaway wait** — 500 ms under the roof (and again after walking out) so `updateRoofCutaway` eases to FADE_DEFAULT / FADE_TURRET at FADE_RATE 5.

## Result

`npm test -- t13 --jobs 1` → **8 pass, 0 fail**.

## Next

GB-1 t15.
