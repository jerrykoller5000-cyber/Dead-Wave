# cursor — CU-29 the day-1 last-kill stall — 2026-09-25
Changed: No game code. Found the cause of the day-1 last-kill stall: two shader variants are compiled for the first time a couple of seconds into the first finisher. That is about 1 s of shader compile and link on one frame, once per page load.
Files: handoffs/2026-09-25-cursor-CU-29.md
Tests: three headless probes (fresh load, day 1, drain the wave plan, kill all but one, then kill the last and record 30-40 frames):
- CPU profile: one frame of 1200 ms. Its self time is `getUniformBlockIndex` 665 ms and `getProgramParameter` 393 ms, which is WebGL linking a new program. `killZombie` itself took 4 ms.
- The same finisher on day 2: no frame over 470 ms, and no compile time. The cost is only the first time.
- The renderer's node-builder cache grows 79 to 81 across the finisher. Both new variants are lit materials with an emissive colour, about 32 KB of fragment shader each. The stall frame comes 11 to 14 frames after the kill (about 3 s of wall time headless), not on the kill.
- Eight materials are new to the scene after the kill. Five are `MeshStandardMaterial` with emissive `3a0505` at 0.18 (dark red: gore or flesh), one is emissive `fff2c8` at about 0.41 (pale glow, likely the GB-42 skull recall), one is the zombie eye green `88ff44` at 1.6, and one is a `MeshStandardNodeMaterial`.
Screenshots: none.
Not verified: which two of those eight need the new programs; which fixes it on Jerry's GPU, where the compile is shorter (AG-15 saw 373 ms). The finisher camera and red flash are not it: the flash is a DOM overlay, and no scene object was drawn for the first time.
Requests: Grokbot, add the finisher's materials (gore at emissive 0.18, the skull recall glow, a detached head's eyes) to the staged pre-roll fight so they compile behind the menu. Claude, for review.
Contract changes: none.
