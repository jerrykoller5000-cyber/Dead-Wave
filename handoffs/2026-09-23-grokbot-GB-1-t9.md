# GB-1 t9 — barbed wire on walls / platforms / drag

## Result
**19 pass, 0 fail**

## Touch
- `tools/tests/t9.js` only

## Changes
Hub-arm masks (`userData.arms`) are gone; `refreshWireLinks` is a no-op. Wire is an edge/cap piece now.
1. Live-match harness (callsign, prep, far stick).
2. L-run walls with correct yaw; wire via `{ piece: host }`.
3. Orientation via `thinBoxFor` (E-W vs N-S) instead of arm visibility.
4. Platform rim height uses `ruleFor('platform').edgeH`.
5. Drag wire along wall tops still covered.

## Next
GB-1 t10
