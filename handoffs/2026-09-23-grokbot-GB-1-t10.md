# GB-1 t10 — wall drag, thin boxes, caps, door/window, dependents

## Result
**30 pass, 0 fail**

## Touch
- `tools/tests/t10.js` only

## Changes
1. Live-match harness + leveled pad (random open-block search threw when match never started).
2. Platform / floor use `{ lv: 1 }` / `{ forceLv: 1 }`.
3. Window cut via `{ piece }`; sandbag ground line uses yaw 0 (south edges) so thin boxes abut on X.
4. Kept door aim/E/open, wire-on-wall, dependents scrap, mortar lean.

## Next
Check BOARD for remaining GB-1 / GB-6 (t19, t36, t37).
