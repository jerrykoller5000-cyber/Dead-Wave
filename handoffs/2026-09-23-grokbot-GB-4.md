# Grokbot — GB-4 caveWarn + cave roles — 2026-09-23

Changed:          Wave director now drives Claude's `caveWarn(cave, level)`:
                  **1** in `startPrep` after the preview freezes (active mouths from
                  `getActiveCaveIndices()`); **2** in `beginWave` for all active mouths
                  (chosen over per-first-spawn — matches CL-4 "2 as its wave starts" and
                  keeps surround coherent); **0** via `warnActiveCaves` clear-all when
                  re-entering prep / `nodead`. Drowned (`caveIndex === -1`) never get a
                  fake warn. Added **`CAVE_ROLES` / `roleForCave` / `applyCaveRole`**
                  (combat-phase1 §2): wet→douse, iron→armoured, root→climber,
                  shale→cleft, chalk→bone, hill→barrow. Traits applied on successful
                  cave spawn (`spawnWaveBatch`); drowned stay aquatic-only; caveguard
                  not in horde table. Phase-1 mechanics: speed/armor/hp/fireResist/
                  headshotMul stamps; climbers vault low cover (`CLIMB_HEIGHT` 1.1) in
                  hard collision; douse ages nearby `groundFires`. Did **not** touch
                  ChatGPT HQ briefing shop HUD, t34, D-8 purchase-delivered, or GB-5.
Files:            `index.html` (wave director + zombie spawn / damage / collision only);
                  `tools/tests/t48.js` (new);
                  `handoffs/requests.md` (Claude caveWarn → DONE)
Tests:            `npm test -- t48 --jobs 1` → **20 pass, 0 fail** (~9.3 s CT);
                  `npm test -- t43 --jobs 1` → **33 pass, 0 fail** (~8.8 s CT) smoke.
Screenshots:      none (director + data-plane).
Not verified:     Live GPU dust/eye feel at warn 1→2; flow-field still treats low cover
                  as blocked for climbers (collision vault only — Phase 1 approx);
                  preferred type bias from cave theme not reweighted in composition.
Requests:         ChatGPT: `'dw-cave-warn'` already fires from Claude — minimap pulse
                  (GP-5). t34 briefing alarm update stays **LATER after GB-4** (this
                  checkout). D-8 `purchase-delivered` still LATER.
Contract changes: Director calls `caveWarn` via `warnActiveCaves(level)`:
                  clear-all to 0, then set `getActiveCaveIndices()` to level (skip `<0`).
                  Level-2 policy: **at `beginWave`**, not first successful spawn per cave
                  (handoff mark for CL-4 / D-8 "batch spawns" — wave start is the batch
                  gate). Exports on `TT`: `CAVE_ROLES`, `roleForCave`, `applyCaveRole`,
                  `CLIMB_HEIGHT`, `warnActiveCaves`. Spawn stamps `z.caveTrait` /
                  `z.caveTheme`.

## Level-2 policy (handoff mark)

**`beginWave` → warnActiveCaves(2)** for every index in `getActiveCaveIndices()`.
Rationale: CL-4 wording ("2 as its wave starts"), surround days light all mouths in
one pulse, idempotent with Claude's no-op on same level, and t48 asserts it without
driving a full spawn drain. Per-cave first-spawn would desync surround and need
extra bookkeeping.

## Cave roles (Phase 1)

| theme | trait | mods |
| --- | --- | --- |
| wet | douse | speed×0.85, fireResist+0.22, damp groundFires in radius |
| iron | armoured | armor+0.10, speed×0.92 |
| root | climber | vault builds with height ≤ 1.1 (not wall/gate/door/pillar) |
| shale | cleft | speed×1.08, prefer tactics `flank` if was `direct` |
| chalk | bone | hp×0.90, headshotMul 1.35 |
| hill | barrow | hp×1.08 |

## Next
t34 GP-5 alarm expectation (LATER); D-8 purchase-delivered; GB-5 / GB-9 as board.