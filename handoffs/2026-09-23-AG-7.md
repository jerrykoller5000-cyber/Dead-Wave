# antigravity — AG-7 ChatGPT screens: GP-7 prep checklist + GP-9 objectives fixture — 2026-09-23

Changed:          Verified GP-9 objectives fixture on Jerry's GPU: 7 markers render at desktop (1280px) and narrow (390px); HUD tracker fits within the 390px viewport (right=372, no crop). GP-7 prep checklist DOM is present in a live match but goals list showed empty (Prep 0/0) and the element was hidden throughout the test; goals did not populate in debug modeHunt/raf=timer entry. Reported to ChatGPT.
Files:            qa/run-ag7.mjs, qa/shots/2026-09-23-AG-7/*.png, qa/2026-09-23-AG-7.md
Tests:            node qa/run-ag7.mjs → GP-9: 7 markers found, tracker not cropped (right=372 < 390). GP-7: checklist element present, goals list empty (0/0), hidden throughout prep in modeHunt debug entry.
Screenshots:      qa/shots/2026-09-23-AG-7/gp7-prep-start.png, gp7-bank-ticked.png, gp7-ammo-refill.png, gp7-repair-pending.png, gp7-repair-done.png, gp7-alarm-ticked.png, gp7-hq-mirror.png, gp9-desktop-default.png, gp9-desktop-selected.png, gp9-mobile-default.png, gp9-mobile-selected.png
Not verified:     GP-7 checklist goal population: #prepChecklist showed hidden at prep start and Prep 0/0 (empty ul.prep-goals) after bank deposit and alarm. Wall placement failed (tryPlace returned count=0 at gx=0,gz=-2 — likely occupied cell), so repair goal could not be exercised. Phase stayed "prep" after alarm trigger in raf=timer mode. ChatGPT's own node ui/browser-checks.mjs --prep passes headlessly; the discrepancy is likely that publishPrepState is not called on debug/direct modeHunt entry without a full insertion cycle. Real insertion needed to confirm live ticks. GP-9 state selector not found by queried IDs (objectiveState/stateSelector); only default and first-click-selected states captured.
Requests:         ChatGPT: GP-9 fixture 7 markers confirmed at desktop and 390px, tracker not cropped. GP-7 checklist not verified live — element present but goals empty (0/0) and hidden in modeHunt debug entry. Please advise if publishPrepState requires a full insertion cycle (controls-ready) rather than direct phase jump, or if there is a debug trigger I should call first. Will re-run once confirmed.
Contract changes: none

## Detailed Observations

### Part A — GP-7 Prep Checklist (live match)

Entry method: `modeHunt` click from title → `TT.getPhase() === "prep"` reached in ~8 s.

| Step | Finding |
| --- | --- |
| Prep start | `#prepChecklist` found in DOM but `hidden=true` |
| After bank deposit | `<summary>Prep 0/0</summary><ul class="prep-goals"></ul>` — goals list empty |
| Wall placement for repair test | Failed: `tryPlace('wall', 0, -2)` returned `count=0` (cell occupied or invalid in current world state) |
| Alarm trigger | Phase stayed "prep" after alarm in raf=timer mode |
| HQ mirror | Shot captured (`gp7-hq-mirror.png`, 378 KB — notably smaller, HQ panel view) |

**Key finding:** The checklist element exists and renders correctly when queried, but `publishPrepState` was never called — `ul.prep-goals` stayed empty throughout. ChatGPT's `node ui/browser-checks.mjs --prep` passes by injecting a stub prep snapshot directly; that path works. The live path may require `controls-ready` to fire (i.e., a real insertion completion) before the checklist arms itself. This is not a regression claim — just not verified in this entry mode.

**Shot notes:**
- `gp7-prep-start.png` (726 KB): Full game world at prep, checklist hidden
- `gp7-bank-ticked.png` (873 KB): Bank deposited, HQ window interaction succeeded, checklist still hidden/empty
- `gp7-alarm-ticked.png` (776 KB): Post-alarm state; HUD visible
- `gp7-hq-mirror.png` (378 KB): HQ panel view, smaller file = tighter framing

### Part B — GP-9 Objectives Fixture

#### Desktop (1280×720)

| Check | Result |
| --- | --- |
| Markers rendered | **7** ✅ (expected 7) |
| Default state screenshot | `gp9-desktop-default.png` (125 KB) |
| First marker selected | `gp9-desktop-selected.png` (133 KB) — selection visually distinct |
| State selector cycling | Not exercised — `#objectiveState`, `#stateSelector`, `select[name=state]`, `select[data-state]` all returned null; selector may use a different id/class |

#### Narrow (390×720)

| Check | Result |
| --- | --- |
| Default state | `gp9-mobile-default.png` (50 KB) |
| First marker selected | `gp9-mobile-selected.png` (56 KB) |
| HUD tracker crop | `right=372 px`, viewport=390 px → **not cropped** ✅ |
| Tracker found | `true` ✅ |

The narrow shots are significantly smaller in file size (50–56 KB vs 125–133 KB desktop) — consistent with a mobile-optimised layout reducing visible elements at 390 px. Markers and tracker readable by visual inspection.
