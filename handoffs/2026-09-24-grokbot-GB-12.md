# Grokbot — GB-12 Floors follow aim (D-12) — 2026-09-24

Changed:          **D-12 policy in game + tests.** Without an elevated aim, a grounded
                  floor in a walled square goes at the player's feet (t13 intent kept).
                  Aim at wall tops / elevated `opts.lv`, or stand upstairs, and it roofs.
                  (a) `resolveTarget` groundDeck: removed "supported elevated beats
                  boardwalk" as the default order; feet-first when not upstairs and not
                  aim-elevated; elevated/upstairs still prefer supported roofs and refuse
                  quiet boardwalk on elevated aim (GB-8). (b) `placeOpts`: pass `pt.lv`
                  even when the thin wall mesh was not hit (ray height path from
                  `getPlacePoint`), so tryPlace/ghost match drag's wall-top aim.
                  (c) t5/t11/t12 roof cases that relied on the old default now pass
                  `{ lv: 1 }`; t6 already aimed at wall tops (unchanged); t13 unchanged.
Files:            `index.html` (builds only: `resolveTarget` groundDeck, `placeOpts`);
                  `tools/tests/t5.js`, `t11.js`, `t12.js`
Tests:            `npm test -- t13 --jobs 1` → **8 pass, 0 fail** (~26.7 s CT);
                  `t5` → **31 pass, 0 fail** (~16.6 s CT);
                  `t6` → **20 pass, 0 fail** (~30.0 s CT);
                  `t11` → **13 pass, 0 fail** (~7.8 s CT);
                  `t12` → **22 pass, 0 fail** (~26.2 s CT).
Screenshots:      none (harness placement / aim probes).
Not verified:     Live GPU feel on steep slopes (AG-3 already checked roof-when-aimed);
                  boardwalk under an open tower still allowed when not aiming elevated.
Requests:         none.
Contract changes: Floor default without elevated aim / upstairs = **feet** (D-12).
                  Roof tests must aim (`setAimRay` or `opts.lv` / `forceLv`). Check out
                  with `--review` as required for GB-12.

## Board

GB-12 done. Next: **GB-14** guardian night (uses GP-10 keys). Unblocked after this
checkout. ACK D-15: freeze off, slice-by-slice split.
