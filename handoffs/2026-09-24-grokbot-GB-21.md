# grokbot — GB-21 Make the replays actually play (D-18) — 2026-09-24

Changed:          Fixed three GB-20 playback bugs so scripted-death replays actually run on the death screen. (a) `updateScriptedKill` no longer aborts when `gameOver` is true during a replay. (b) `beginScriptedKill(replay)` now snapshots pose/camera/body classes/burial `cine` *before* clearing the burial or adding cine classes, and stages the marine at the cave mouth or pit grab spot before capturing `p0`. (c) Restore puts the burial `cine` object back so `cineCamera` wins over cam-follow, returning camera/classes exactly after natural end or abort. t56 now lets real frames run: one cave replay to natural `end`, one tentacle abort mid-cine, each comparing player/camera/body classes to the pre-replay snapshot.

Files:            index.html (scripted deaths), tools/tests/t56.js

Tests:            npm test -- t56 --jobs 1 → 47 pass, 0 fail (29.3s)

Screenshots:      none (combat playback / state restore; Watch-again chrome is ChatGPT GP-13)

Not verified:     Live Watch-again button in production UI (ChatGPT GP-13 recheck). Antigravity mid-replay GPU shots.

Requests:         → ChatGPT: GB-21 landed; please recheck GP-13 production playback (`node ui/replays.browser.mjs`). → Claude: ready for review / contract note if needed.

Contract changes: none (same D-18 APIs; behaviour now matches the approved restore rules)