# Grokbot - GB-14 Guardian night (D-13) - 2026-09-24

Changed:          Fightable `guardian` night per combat-phase2 + D-13.
                  (1) Schedule `day >= 6 && day % 6 === 0`: pushes one `guardian`,
                  replaces surround + colossus; Blood Moon still stacks.
                  Source: chalk cave only (`findGuardianCaveIndex`, hill fallback).
                  (2) `ZOMBIE_TYPES.guardian` (HP 420, boss tactics) kept separate from
                  immortal `caveguard`. Gaunt mesh dress; skip chalk bone `hpMul`.
                  (3) D-13 `caveWarn`: 1 at prep / 2 at beginWave / 0 via clear path
                  (same as every night � not prep stuck at 2).
                  (4) Stuck failsafe: no progress toward player for 60s ? re-path; if
                  that fails, retreat to chalk mouth.
                  (5) `getWavePreview` additive: `hasGuardian`, `guardianNight`,
                  `guardianCaveIndex`, `guardianCaveTheme`, `guardianKeys` (GP-10).
                  Banners use `dwText(GUARDIAN_KEYS.*)` � no hard-coded player copy.
                  (6) Win = kill guardian + clear planned wave (existing clear path).
                  (7) First-blood: `publishUI('guardian-first-blood')` once per run on
                  player-credit kill; economy owns receipt (mortar blueprint free OR
                  +80 skull value � never direct Cash).
                  (8) Near `MAX_ZOMBIES`, pending guardian is promoted to next so fodder
                  cannot starve the reserved slot. `trimGuardianFodder` ~20% shamblers.
                  (9) APIs: `isGuardianNight`, `getGuardianState`, `GUARDIAN_KEYS`,
                  exports `startPrep`/`clearZombies`/`ZOMBIE_TYPES`/`MAX_ZOMBIES`.
Files:            `index.html` (wave director, zombies); `tools/tests/t53.js` (new);
                  `handoffs/requests.md` (GP-10 DONE + ChatGPT economy ask).
Tests:            `npm test -- t53 --jobs 1` ? **57 pass, 0 fail** (~14.5 s CT);
                  `t43` ? **33 pass, 0 fail**; `t48` ? **20 pass, 0 fail**;
                  together **110 pass, 0 fail**.
Screenshots:      none (harness).
Not verified:     Live GPU feel / minimap pulse intensity (ChatGPT); economy receipt
                  delivery and kiosk refresh; Claude `contracts.md` API record.
Requests:         ChatGPT: wire `guardian-night-first` receipt (blueprint or +80 skulls).
                  Claude: record `getGuardianState` / first-blood event in contracts.md.
Contract changes: Additive WavePreview fields; `isGuardianNight`; `getGuardianState`;
                  `dw-game` types `guardian-first-blood` / `guardian-killed` (propose only).

## Board

GB-14 done. Queue empty for Grokbot until Claude assigns next. Waiting on ChatGPT
economy receipt + Claude contracts. ACK D-15 freeze off.
