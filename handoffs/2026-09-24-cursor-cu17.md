# cursor — CU-17 bench scenarios — 2026-09-24
Changed: `tools/bench.mjs` now has `--scenario day5` (set day 5 and start the wave) and `--scenario build` (place ten walls through `setPlaceMode`, `setAimRay`, and `beginPlaceClick`). The default is still megaswarm.
Files: tools/bench.mjs
Tests: `node tools/bench.mjs --scenario day5 --seconds 2 --headless` reached day 5, phase wave, and printed the four lines. The build scenario uses the same place calls as t6. Full 30 s GPU runs are Antigravity's (AG-9b).
Screenshots: none.
Not verified: the ten-wall scenario on a live GPU; the 2 s headless day-5 print was fps 0 because the window was too short to fill a one-second sample.
Requests: Antigravity, the two commands.
Contract changes: none.
