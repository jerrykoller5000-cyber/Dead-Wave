# chatgpt — GP-14 Remove replay controls (D-20) — 2026-09-24
Changed: Removed the remaining replay stylesheet link, replay UI module/CSS, replay-only browser test and ten replay strings. GB-22 had already removed its mount/import; the death catalogue and live death scenes remain.
Files: index.html (death-screen UI stylesheet), ui/strings.js, deleted ui/replays.js, ui/replays.css, ui/replays.browser.mjs.
Tests: `node --test ui/*.test.mjs` → tests 69, pass 69, fail 0. `rg` for mountDeathReplays/watchAgain/replay-tile/replays.js/replays.css/replay string keys in index.html and ui returns no matches. recordDeath/loadDeathLog and catalogue remain. npm test not run (AGENTS CDP limitation).
Screenshots: Previous controls: Claude outputs/shots/gp13/death-screen.png. After-removal browser shot will be captured in the next UI integration check; tools/shoot unavailable here.
Not verified: Real GPU, frame/load budgets and shared suite. Browser integration also needs the newly introduced .45 label mapping, handled in GP-15/18, not changed in this task.
Requests: Cursor to include this D-20 deletion with GB-22; Antigravity QA is deferred while out of usage per board.
Contract changes: None new; D-20 already withdraws replay APIs. Removed feature-only test deliberately per GP-14, not to hide a failure; lead review requested.
