# cursor — CU-30 title times and the real GPU — 2026-09-25
Changed: `tools/cdp.mjs` only forces SwiftShader for headless runs. A visible window now uses Jerry's real GPU and WebGPU. Before this, every "real GPU" number from `tools/*` and `qa/*` (Antigravity's AG-9, AG-9b, AG-11, AG-15, and my title times) was software rendering through WebGL2.
Files: tools/cdp.mjs (and index.html for the favicon, in 0cff997)
Tests, all on Jerry's GPU in a visible window after the fix (renderer reports WebGPU):
- `node tools/loadtime.mjs --runs 2` → title at 8.0 s and 7.0 s warm. Largest stages: "post + pools + compile + warm frame" 2.3 to 2.8 s, "POIs + minimap + foliage chunks" about 1.1 s, and 6.5 to 7.2 s before `window.TT` exists.
- Title menu: 57 to 60 fps every second after the first (the first second is 9). The AG-15 "45 fps" was the software renderer.
- `node tools/bench.mjs --seconds 15` (500 shamblers): fps 59.4, 1% low 49.7, worst 25.3 ms, 8 hitches.
- `node tools/bench.mjs --scenario day5 --seconds 15` (221-zombie plan with a colossus): fps 59.9, 1% low 53.9, worst 17.2 ms, 0 hitches.
- Before the fix, the same visible-window loadtime read 6.2 s cold and 6.1 s warm on the software path.
Screenshots: none.
Not verified: a cold title time on WebGPU (only warm runs after the fix). The 5 s warm budget is still missed on the real GPU (7 to 8 s); no load change was made in this task.
Requests: Claude, the performance priorities and CU-29's stall numbers came from software rendering and should be re-read against these.
Contract changes: none.
