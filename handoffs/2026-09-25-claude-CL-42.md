# claude — CL-42 day 1's song, as good as it can be — 2026-09-25
Changed:          "First Blood" now follows the wave. It's cut into seven sections, each its own seamless circle at the song's tempo: stalk (new: the engine low, a heartbeat, the hook far off in the echo), dropA, riffB, dropA2, bridge, break and climax. They sit in one Opus file played through Web Audio with sample-accurate loops. The director stalks while the horde is out of sight, cuts to the drop on the next bar line the moment one is within 45 m, rotates dropA, riffB, dropA2, bridge while the fight is on, drops to the break after 4 s with nobody within 70 m, and goes to the climax for the last three. The whole song stays as the fallback on the old <audio> deck.
Files:            core/audio.js (the section player: `SECTIONED`, `loadSections`, `secPlay`, `secUpdate`; `startDeck`/`stopDeck`/`startFight`, the levels, `musicState`), index.html (the audio state gets `remaining` and `spawnedAll`), tools/day1.py, assets/soundtrack/fight_day01_sections.ogg (new, 4.7 MB), fight_day01.mp3, fight_day01.mid, music.json (`sections`), tools/tests/t73.js, tools/tests/t60.js
Tests:            t73 12/0 (new): decoded before the wave, played from the section file, stalk, loop bounds exact, drop within 45 m, break when quiet, back to riffB, climax for the last three. t60 32/0 (drains the plan before its proximity step, Grokbot's request for D-29). Chrome decodes the Opus file to exactly 240.000 s at 48 kHz with no leading silence. Section levels: stalk -20.5, drops -14.5 to -14.3, break -18.5, bridge -14.0, climax -13.4 dB RMS, peaks at 0.94 or below.
Screenshots:      none
Not verified:     how it sounds. I judged it by structure, levels and spectra; Jerry's ears decide. The numbers to change are the `flow` block in music.json (near 45, far 70, lullAfter 4, lastAt 3) and the section levels at the bottom of tools/day1.py.
Requests:         none
Contract changes: `music.json` gains `sections` (documented in its `about`); `AudioSys.musicState()` gains `section`, `sectionMode`, `sectionLoop`, `sectionsReady`.
My audit's CL-A4, corrected: the 12 ms at the song's end is the bass's own articulation gap, and the "jump" is the downbeat kick. The real seam risk was `<audio loop>` on an mp3, which the section player doesn't use.
