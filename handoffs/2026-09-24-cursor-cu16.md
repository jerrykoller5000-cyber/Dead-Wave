# cursor — CU-16 measure the fight — 2026-09-24
Changed: Nothing in the game. Added `tools/profile.mjs`, which spawns 40 shamblers, samples frame times, places 8 walls, and reads the pathfinding cost the game already stores.
Files: tools/profile.mjs
Tests: `node tools/profile.mjs --headless` spawned 40 and placed 8 walls. Frame times were the headless timer (about 1.5 fps, worst frame about 1200 ms), not Jerry's GPU. Flow-field cost stayed 0.0 ms before and after the walls.
Screenshots: none.
Not verified: a day-5 fight on the real GPU. Antigravity is on that (AG-9). This run does not explain the 45-to-30 drop.
Requests: none.
Contract changes: none.

The placement hitch guess does not match the code. `updateFlowFields` runs on a timer inside `updateZombies`, and again when a tree falls (`dw-log`). `spawnBuild` and `tryPlace` do not call it. Placing a wall does not rebuild the paths.
