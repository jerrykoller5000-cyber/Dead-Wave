# cursor — CU-8 npm test trust — 2026-09-23
Changed: Each behaviour check opens in its own window and is brought to the front, so the page is not a hidden tab. `tools/tests/lib.js` provides `startMatch(T, name)` (callsign, Play, prep, then the ~9 s insertion) and the runner evaluates it into the page before the check. Every run writes `crew/tests.json` for the panel; a named run updates only the files it ran. ChatGPT's `CDP timeout: Page.enable` is his environment — `AGENTS.md` now says he marks the suite not run and Cursor runs it at commit time. The hidden-tab pump (CU-8 a) landed with CU-2.
Files: tools/cdp.mjs, tools/tests/run-all.mjs, tools/tests/lib.js, crew/tests.json, AGENTS.md
Tests: `node tools/tests/run-all.mjs t45 --jobs 1` → 8 pass, 0 fail, 7.0 s, and the page's visibilityState was `visible`. Full `npm test` not re-run. New helper: `startMatch` in tools/tests/lib.js (no check calls it yet).
Screenshots: none. The runner is headless.
Not verified: a full suite under the new window-per-check launch; ChatGPT's machine (the timeout is not reproducible here — t45 reached `Page.enable` and passed).
Requests: none.
Contract changes: none.
Commit: index.html is still uncommitted from CU-2 because Grokbot and ChatGPT are in other parts of it.
