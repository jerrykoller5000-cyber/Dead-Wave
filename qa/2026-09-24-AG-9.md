# Antigravity — AG-9 Real-GPU numbers — 2026-09-24

> Run 2026-09-24 06:53 UTC on Claude Sonnet 4.6 (Thinking). Real browser, Jerry's GPU.
> CU-15 honest FPS counter: avg fps, 1% low, worst-frame ms, hitches (>50 ms).

## Results

| Scenario | Avg FPS | 1% Low | Worst frame | Hitches | Valid? |
|---|---|---|---|---|---|
| Megaswarm (500 shamblers, 30 s) | **1.6** | **1.6** | **634.6 ms** | **58** | ✅ Real |
| Day-5 fight (20 s live combat)  | 0.0 | 0.0 | 0.0 ms | 28 | ❌ See below |
| Build pieces (10 walls placed)  | 0.0 | 0.0 | 0.0 ms | 30 | ❌ See below |

Budget: 60 fps avg · worst frame <50 ms at 48 zombies · 60 fps standard view.

## Analysis

### Megaswarm — REAL ✅

`TT.spawnMegaswarm(500)` worked. 500 shamblers spawned and ran for 30 s.

- **1.6 fps average** — catastrophic but expected for 500 zombies past the cap. This is the worst-case chaos number.
- **634.6 ms worst frame** — over 12× the 50 ms budget.
- **58 hitches** in 30 s — nearly 2 per second.

This is the baseline for CU-16's profiling work: Cursor should identify what is burning those frames with this many zombies.

### Day-5 fight — NOT VALID ❌

`TT.debugDay` does not exist in this build (`method: "none", day: "?"`). `TT.beginWave()` was called and returned true, but no zombies were spawned (zombie count: 0 after 3 s). Without active zombies, `TT.perfSnapshot()` returned zeros for fps/low/worst. The 28 hitches are real accumulated frame stalls, but cannot be attributed to combat.

**What Cursor needs to give me:** the correct console API to (a) advance to day 5 and (b) trigger the night wave with zombies already spawned. I will re-run once I have it.

### Build pieces — NOT VALID ❌

Neither `TT.debugPlace` nor `TT.tryPlace` exist. Placements logged with positions but no `ok` field confirmed — no walls were actually placed. `TT.perfSnapshot()` again returned zeros (idle game loop).

**What Cursor needs to give me:** the correct API to place a build piece via the console. I will re-run once I have it.

## Screenshots

Megaswarm start (500 shamblers spawned, game still loading positions):
![megaswarm start](qa/shots/2026-09-24-AG-9/megaswarm-start.png)

Megaswarm after 30 s of full combat:
![megaswarm end](qa/shots/2026-09-24-AG-9/megaswarm-end.png)

Day-5 attempt (wave triggered, but 0 zombies — scene idle):
![day5 start](qa/shots/2026-09-24-AG-9/day5-fight-start.png)
![day5 end](qa/shots/2026-09-24-AG-9/day5-fight-end.png)

Build before and after (no actual placements confirmed):
![build before](qa/shots/2026-09-24-AG-9/build-before.png)
![build after](qa/shots/2026-09-24-AG-9/build-after.png)

## Not verified

- Day-5 fight FPS: could not advance to day 5 or spawn zombies (`TT.debugDay` missing).
- Build-piece hitch: could not place walls (`TT.debugPlace` / `TT.tryPlace` missing).
- `TT.perfSnapshot()` returns zeros when game loop is idle — need active wave for valid readings.

## Requests sent

- → Cursor: AG-9b request for correct debug APIs (day advance + wave spawn + build place).

## Run details

- Script: `qa/run-ag9.mjs`
- Shots: `qa/shots/2026-09-24-AG-9/`
- All scenarios: headed Chrome (real GPU), 1280×720, `?debug=1&raf=timer`