# CU-35 · day 1 twice on Jerry's GPU · 2026-09-25

Visible Chrome window on Jerry's PC, NVIDIA GeForce RTX 5080, renderer WebGPU (the in-game overlay says so). Script: `qa/run-cu35.mjs`. Shots: `qa/shots/2026-09-25-CU-35/`. Two full passes; the numbers below are from the second, with the first where it differed.

## Timings

| Moment | Time |
| --- | --- |
| Page load to the menu, splash played through by itself | 14.2 s (14.1 s first pass) |
| Alarm to the first zombie on the map | 3.0 s |
| Alarm to first contact (a zombie within 3 m of the marine) | 16.8 s (19.0 s first pass) |
| First scripted shot to the last kill, 15 kills | 18.2 s |
| Last kill to day-2 prep (the finisher hands back) | 9.5 s |

Day 1's plan was 15 shamblers: 8 rising from the ground, 7 from one cave (East Cave in the second pass, Northwest Cave in the first). The day-1 POI guards were there (3).

## Frame rate

| Phase | Mean fps | Lowest 1% low | Worst frame |
| --- | --- | --- | --- |
| Menu | 60.0 | 56.2 | 17.8 ms |
| Prep | 60.0 | 59.5 | 16.8 ms |
| Wave, zombies on the marine | 60.0 | 59.2 | 16.9 ms |
| Finisher | 59.9 | 50.3 (39.4 first pass) | 19.9 ms (25.4 ms first pass) |

## What I saw

1. Day 1 holds 60 fps from the menu to dawn on this GPU. The finisher is the only dip.
2. The first time the goggles go on in a session, the overlay showed a 200 ms frame and a 1% low of 5. Later toggles held about 18 ms. That looks like the goggles' post effect compiling on first use, the same kind of one-time stall as CU-29. Worth adding to the warm-up.
3. A real death shows the death screen; Play again starts day 1 with Shambler kept in the ways-to-die list (D-31).
4. 0 page errors in the second pass.

## What I could not check

- The kills were scripted (one `killZombie` every 1.2 s), not aimed and fired, so "first shot to last kill" is the length of the plan at that kill rate, not a player's time.
- The marine was in godmode during the wave so first contact could be timed without dying.
- The night-cave shots (06 to 09) show the East Cave's hill and boulders at 30 m and 10 m, goggles off and on, from the play camera. The mouth itself is not clearly in frame: the play camera looks down from behind the marine and I did not steer it. Claude may want a fixed camera for CL-48.
- The cash after Play again read 999,999 because my run used the `bigtex shooter` dev cheat in run 1 and it carries over Play again (it resets on the pause menu's reset, not on Play again). Dev-only.
- Antigravity's own `qa/run-ag15*.mjs` were not run: they write into the AG-15 shot folder, and this script covers the same steps on the WebGPU path Jerry plays.
- No audio was checked: the window is muted.
