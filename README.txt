Tiny Trek — FIDELITY UPDATE (UAL anim pack)

Run "Play Tiny Trek.bat" (serves this folder on port 8766).

Assets:
  assets/UAL1_Standard.glb  — Quaternius Universal Animation Library (CC0)
  assets/UAL1_License.txt

Requires network once for Three.js CDN (three@0.160 + GLTFLoader).
GLB is local relative path — must be served via the bat HTTP server (not file://).

Add ?debug=1 to the URL for an FPS / draw-call / triangle overlay.
